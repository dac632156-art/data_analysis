1.数据上传，
	1.目前生成环境受免费的部署影响，为了保证后端内存不崩，所以限制最多5人同时用，并且上传文件不大于30M
	2.设计可以单窗口可上传多个数据表，后面对多数据表要进行“异步+多进程”来处理
2.数据清洗，
	【LLM只做决策，具体用什么方法清洗，由后端来，不给LLM乱来的机会】LLM接收前端要求，然后加上写好的prompt规则（prompt约束），
	限定只能对缺失值，异常值，重复值，数据类型转换进行提修改意见，然后后端接收json结构的分析，对数据进行清洗。
	兜底机制：（1）在后端的每一次清洗，都拍一次快照，用户可以回撤到任意一步；（2）后端再每次成功清洗后都会拍下最新的快照存在df里，当LLM给的决策中，后端函数无法进行时，会跳过本次清洗，并用最新的快照数据继续清洗或者返回给用户；（3）异常跳过，当出现异常时，会记录，然后跳过并将数据退回（2）。
3.数据洞察，
	用户点击分析时，后端会先对清洗好的数据进行列名的映射，然后检查多表中是否有相关连的外键（判断外键的方式是列名相同且取前30行数据值一致），有的话进行多表合并成一张表，没有的话就多表保持不动；将表的列名以及少量数据样本发给LLM（把整个数据给LLM做决策，会消耗大量token并且上下文有限制），
	LLM接收列名和数据样本后，会先看列名在映射词典中有没有出现，如果有出现，LLM看数据样本，判断这个列名有没有映射对，如果错了，LLM纠正，更新这列的列名（例如两张表里面都有name，一个表示用户，一个表示商品）；如果没有出现，LLM自己根据这个列名和数据样本，判断他是否是映射词典中的某一列，如果是，那就将列名变成映射词典中的规范列名，并且将这个新的列名形式添加到对应的映射词典中，
	根据规范后的列名，LLM进行在RAG检索知识库，看列名在知识库中的模板是对应上的，可以进行哪些分析，筛选出所有可以进行分析模型后，LLM进行决策要用哪些分析组合，输出多条分析（带顺序）以及夹带要用到的列，
	然后后端接收决策，根据决策生成图片（后端代码生成，确保数据准确性，LLM生成可能会导致数据不准确），
	{暂时不知道要不要：：：并且LLM的决策分析中后端匹配不到的部分，后端返回给LLM，LLM自己写python，（LLM只写python，他拿不到数据，可能还是得后端来生图）【分成大部分决策可以后端生成，小部分后端没有，LLM生成】}
4.数据洞察后的对话框，跟ai
	5.后端生图，接收LLM的分析结果后，完成分类器校验





# 映射问题
	由于会出现客户表和订单表中都会有name，代表的意思有歧义，放进映射词典不知道放进哪个
	解决方案：
	上传数据后，将每张表的列存下来，然后根据这个列名组合生成一个指纹（哈希值），
	先对没有歧义的列名进行映射（就是先让列跟映射词典里有映射）
	然后对已经保存起来的哈希值一一匹配，有就跟着之前的这个表进行映射
	没有匹配到哈希值，新表的列，跟之前存的列的数组进行一个包含度的计算，包含度达到80%或以上，认定新表里面的列名跟这个表的映射一样
	包含度完全没有匹配到，在从头跟所有数据进行相似度计算，相似度大于70%时，按这个表的映射来
	当包含度或者是相似度匹配到了多个结果满足阈值的情况，谁更大就跟谁映射；当两个或以上的结果相同时看这些表中的有歧义的列名映射是不是一样的，一样的那就用，不一样就得给LLM
	当相似度也没有匹配上，这个时候将需要映射的列名给LLM，列映射完后，这个表的映射编码要存入数据库中：指纹->映射关系”。
	数据库表fingerprint_mapping结构如下:
	fingerprint_id :"alb2c3d4..."
	"id":
	'standard_product_id"}
	mapping_json :{"name": "standard_product_name