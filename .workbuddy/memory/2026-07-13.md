# 2026-07-13 工作日志

## 任务：为简历梳理 DataMind AI 项目架构
用户要求以"简历专家"视角完整理解项目（前后端、架构、规则、引擎、模块、功能链路），为后续写简历做准备。

### 项目全貌（DataMind AI - 数据分析智能体）
- 三层架构：React/Vite/TS 前端 + FastAPI 后端 + Python 自研分析引擎(src/)
- AI 模型：DeepSeek（原生 openai SDK 直连，非 LangChain，关闭 SDK 重试防超时叠加）
- 部署：后端 Render（uvicorn），前端 Vercel（静态构建 SPA）

### 关键引擎（简历差异化亮点）
1. **分析知识中心(analysis_library)**：11 个 YAML 意图(growth/ranking/comparison/structure/concentration/distribution/correlation/anomaly/retention/proportion/geo)，registry 路由，加意图零代码
2. **分析模板+计算器(analysis_templates/calculators)**：纯数据契约，AnalysisPackage(findings:KPI/Chart/Table)
3. **推理管道(reasoning)**：RuleEngine→BusinessFactGraph→EvidenceEngine(STRONG/MODERATE/WEAK)→LLMReasoner，三段独立、可无 LLM 降级
4. **看板布局引擎(dashboard/layout_engine)**：BusinessGrouper→LayoutSelector(策略规则)→GridAllocator(12栅格/最大余数补满/视觉平衡)，纯规则不读 DataFrame
5. **AI Agent(ai_agent/agent)**：五阶段报告流水线(pandas精确统计→LLM洞察叙事)，多级降级 + 结构化 JSON 兜底解析

### 端到端链路
上传CSV → 数据清洗 → 列分类/意图识别 → 分析执行(Planner+AnalysisLibrary+模板) → 图表(ECharts)/看板(WidgetGenerator+LayoutEngine) → 推理洞察(ReasoningPipeline) → 报告生成(异步无状态+轮询)

### 简历定位（用户已确认）
- 岗位：AI应用/后端·平台开发实习生；中文一页；侧重后端/平台
- 后端真实细节（可追问）：FastAPI 11路由40+端点；SessionManager线程安全单例(Lock)+原始数据pickle落盘防OOM+LRU(50会话)+惰性清理+后台守护线程；AI报告异步task_id轮询规避50s超时+信号量限流+关闭SDK重试+多级降级；Renderer层解耦数据/前端格式

### 重大背景（用户自述，已记入 MEMORY.md）
- **项目全部由 AI Agent 经自然语言生成，用户未手写代码**。因此简历改为诚实定位「AI 辅助开发 / 用户主导设计与架构」，绝不可谎称自研。
- 已重写简历_DataMind_AI实习生.html 为诚实 AI 辅助 + 元技能版；新增 MEMORY.md 记录此约束与答辩硬要求。
- 已完成：面试答辩准备（产出 面试答辩准备_DataMind.md）。已核实产品**未用 RAG**（LLM 仅收结构化已验证结论，非向量检索），以"结构化优于检索"的技术取舍诚实作答；诚实红线：不可谎称手写代码、不可编造 skill/框架名。

### 前端结构
页面：UploadPage/CleanPage/AnalysisPage/DashboardPage；DashboardRenderer(Widget/Grid/Interaction Binder)；ECharts 6 + echarts-gl(3D地图) + data-view 大屏

### 补充：skill 真实身份（用户澄清）
- 用户在 GitHub 开源了 `quality-assurance-sop` skill（Agent 质量保障 SOP），仓库 github.com/linziquan/everything_skill（MIT）。
- 内容：五阶段工作流(完整理解→计划→执行→验证→诚实报告) + 置信度标签(✅/⚠️/🔍) + 根因至少两层 why + 用户验证清单；专治 Agent 的 rush/partial/false-completion。
- 已同步进面试答辩准备_DataMind.md 的 skill/幻觉/改错 三题，作为可验证真实素材（面试可现场打开 GitHub）。

### 补充：答辩文档加"产品/应用层"（用户情绪+HR视角）
- 用户情绪：觉得"项目很垃圾"（根因是"没手写代码"的价值归属焦虑）——已从客观工程复杂度安抚，不贴负面标签。
- 用户诉求：HR/面试官开场关注"项目能干嘛、有什么用"，材料太偏工程视角，需补应用价值层。
- 已在 面试答辩准备_DataMind.md 顶部新增：①「这个项目能干嘛」(一句话定义/给谁用/6步功能清单/与ChatGPT差异化=算数与说话分离不瞎编数字)；②「为什么做」提供3框架A痛点/B学习/C AI协作实验(⚠️必须用户真实动机,不可编造实习经历)；③电梯演讲拆HR产品版+技术版。原三~六章顺延为五~八。
- 待办：用户尚未给出真实项目动机（背景题）；可择机模拟面试。
