# DataMind AI · 数据分析智能体 — 项目介绍

> 一份覆盖**业务**与**技术架构**的整体说明文档。文中所有描述均基于对本仓库源码的实际通读，不虚构。

---

## 一、项目业务介绍

### 1.1 产品定位

**DataMind AI 数据分析智能体** 是一个面向非技术人员的 **AI 驱动数据分析平台（Web 应用）**。它的核心目标是把"上传一张业务表格"到"拿到一份可读的分析结论"之间的全部专业工作——字段理解、数据清洗、统计建模、图表选型、仪表盘编排、报告撰写——自动化完成，让不懂 Python、不懂 SQL 的业务人员也能像数据分析师一样工作。

解决的痛点：
- **门槛高**：传统分析需要写代码或熟悉 BI 工具；
- **选型难**："我该画什么图、对比什么、看什么趋势"需要经验；
- **周期长**：从数据到报告往往要数小时甚至数天；
- **依赖人**：结论质量高度依赖分析师个人水平，且难以复用。

DataMind AI 的做法是：**用一套 YAML 驱动的分析知识库 + 模板化计算引擎，把分析师的经验沉淀为可执行的代码；再用 LLM 把统计结果组织成自然语言洞察与报告**。

### 1.2 目标用户与典型场景

| 角色 | 典型场景 |
| --- | --- |
| 业务运营 / 销售 | 上传月度销售明细，自动看趋势、地区分布、Top 客户、异常订单 |
| 产品经理 | 上传用户行为数据，分析留存、转化结构、渠道对比 |
| 管理者 / 高管 | 进入驾驶舱看核心指标，一键生成可下载的分析报告 |
| 数据分析初学者 | 借助"分析建议 / 洞察"理解数据该从哪里看起 |

### 1.3 四步核心业务流程

```
① 上传数据  →  ② 智能清洗  →  ③ 分析与洞察  →  ④ 可视化驾驶舱 + AI 报告
   Upload         Clean           Analysis            Dashboard / Report
```

1. **上传数据**：支持 CSV / XLSX / XLS / JSON / DB / SQLite；系统自动识别字段类型（数值指标、分类维度、时间维度、地区列）。
2. **智能清洗**：缺失值处理（删除 / 均值 / 中位数 / 众数 / 填充 0 / 填充"未知"）、重复行去除、类型修正、异常值检测（IQR / 离群点）。
3. **分析与洞察**：基于自然语言问题或系统推荐意图，执行增长、排名、结构、占比、相关性、对比、地理、词云、异常等分析，产出图表 + KPI + 业务洞察；AI 还能直接对话、自动给"分析建议"。
4. **可视化驾驶舱 + AI 报告**：把多个分析结果**自动编排成驾驶舱**（含筛选、下钻、联动、高亮交互），并基于已保存的分析包生成结构化、可下载的 AI 分析报告。

### 1.4 四大功能模块与用户价值

| 模块 | 能力 | 用户价值 |
| --- | --- | --- |
| **数据上传** | 多格式导入、会话隔离、字段画像（行列数 / 缺失 / 类型） | 零配置接入任意表格，秒级预览 |
| **智能清洗** | 缺失 / 重复 / 类型 / 异常一站式处理，可逐步回看清洗历史 | 把脏数据变成可信分析基础，过程透明可回溯 |
| **分析可视化** | 14 类分析模板、AI 对话与建议、ECharts 图表（含 3D 地图 / 词云）、结果可保存 | 不用想"画什么图"，系统推荐 + 一键执行 |
| **数据看板 / 报告** | 自动驾驶舱（语义分组 + 布局 + 交互）、异步 AI 报告生成与导出 | 从图表到结论一步到位，可直接汇报 |

### 1.5 两种运行模式（降级容错）

- **无 API Key（纯统计模式）**：仍能完成字段识别、精确统计、图表渲染、驾驶舱生成，洞察与报告降级为"自动统计摘要"。
- **有 API Key（AI 增强模式）**：叠加 LLM 对话、智能洞察、报告撰写、分析建议。

> 这一设计保证了**产品在任何情况下都不会"卡死"**——AI 调用失败会立即走降级路径，而非让用户面对报错。

---

## 二、技术架构与数据流

### 2.1 整体技术栈

| 层 | 技术 |
| --- | --- |
| 前端 | React 18 + TypeScript + Vite 5 + Tailwind CSS 3 |
| 可视化 | ECharts 6、echarts-gl 2.1（3D 地图）、echarts-wordcloud 2.1、html2canvas |
| 状态 / 交互 | React Context（useReducer）、React Router 6、Axios、marked（Markdown 渲染） |
| 后端 | FastAPI + Uvicorn（Python 3） |
| 数据计算 | pandas 2.2、numpy 1.26、openpyxl |
| LLM 接入 | OpenAI 官方 SDK（`openai` 1.59，兼容任意 OpenAI 风格接口） |

### 2.2 系统分层

```
┌──────────────────────────────────────────────────────────────┐
│  浏览器（React SPA）                                           │
│  pages: Upload / Clean / Analysis / Dashboard                  │
│  state: DataContext (useReducer) · 可视化: ECharts / GL / Word │
└───────────────┬──────────────────────────────────────────────┘
                │  REST /api/*  (Axios)   ← JSON (经 sanitize_json)
┌───────────────▼──────────────────────────────────────────────┐
│  FastAPI 后端（backend/）                                       │
│  routers: upload data clean stats chart dashboard              │
│           insights report analysis reasoning                   │
│  services: SessionManager（内存持有 DataFrame + 分析包）        │
└───────────────┬──────────────────────────────────────────────┘
                │  import src.*
┌───────────────▼──────────────────────────────────────────────┐
│  核心分析引擎（src/，纯 Python）                                │
│  analysis_library → planner → analysis_templates → calculators  │
│  reasoning（rule/evidence/llm）→ dashboard → report → ai_agent  │
│  echart_generator / chart_renderer（→ ECharts option）          │
│  防护层：模板四步模式 + utils.json_serializer.sanitize_json      │
└───────────────────────────────────────────────────────────────┘
```

### 2.3 核心模块职责

#### 后端路由（`backend/routers/`）— 11 个模块
- **upload**：文件上传、创建会话、会话内数据存取。
- **data**：数据基础操作与预览。
- **clean**：缺失值 / 重复 / 类型 / 异常清洗接口。
- **stats**：统计计算接口。
- **chart**：图表生成接口。
- **dashboard**：驾驶舱 KPI / 地图 / 图表配置。
- **insights**：AI 自动洞察（结构化 JSON：insights + intents）。
- **report**：**异步报告**——提交即返回 `task_id`，前端轮询状态（规避 Render 免费实例约 50s HTTP 超时），支持无状态（请求体携带 packages）。
- **analysis**：分析执行与保存，含 **Library fallback 自动降级链**（unsupported → YAML fallback → 自动派生列重试）。
- **reasoning**：业务推理管道。
- **SessionManager**：以 `session_id` 为键在内存中持有 `DataFrame` 与已保存的 `AnalysisPackage`。

#### 核心分析引擎（`src/`）
- **`analysis_library/`**：**YAML 驱动的唯一分析知识中心**（V3 已移除所有散落的业务规则）。含 14 类分析意图（anomaly / comparison / concentration / correlation / distribution / geo / growth / proportion / ranking / retention / structure / wordcloud 等），每一项定义关键词、优先级、字段 Schema 要求、默认算法、输出规范、Fallback 策略、自动意图推荐规则。
- **`planner.py`**：**纯调度器**（V3 去业务化）。流程：`Library.lookup(目标)` → 列推断（时间/指标/维度）→ **缺失列自动派生**（计数指标 / 合并日期 / 放宽分类列）→ 生成分析计划；匹配失败时按 YAML fallback 链降级。
- **`analysis_templates/`**：14 个分析模板 + `base.py` 的 **`AnalysisPackage`** 统一输出结构（KPI / insights / conclusions / chart_data）。每个模板遵循"数据计算 → 边界修复 → Insight → 输出 Package"四步模式。
- **`calculators/`**：精确统计计算（趋势、同环比、Top/Bottom、集中度、分布、异常检测）。
- **`reasoning/`**：业务推理管道 = **规则引擎 → 证据引擎 → LLM 推理器**，把多个 AnalysisPackage 提炼为执行摘要、叙述与建议（`ReasoningPipeline.run`）。
- **`dashboard/`**：**自动驾驶舱生成器**。把 AnalysisPackage 转为 `DashboardSchema`，含语义分类（SemanticWidget）、重要性评分（5 维加权）、组合规划（Section 分组 / 阅读流 / 视觉层级）、布局引擎（24 列栅格）、交互引擎（全局筛选 / 交叉筛选 / 下钻 / 高亮 / Widget 联动）。
- **`report/`**：`report_analyzer.py`（五阶段 pandas 精确统计）+ `report_builder.py`（把分析包组织为 AI 报告输入）；`ai_agent` 据此生成结构化 sections。
- **`ai_agent/agent.py`**：`DataAnalysisAgent`——对话、洞察、报告三大能力；基于 `openai.OpenAI` 客户端（base_url / api_key / model 由前端透传）；内置 **Python 代码执行沙箱**（线程超时保护，防死循环）；AI 失败时全部降级为纯统计结果。
- **`echart_generator.py` / `chart_renderer.py`**：`AnalysisPackage.chart_data` → ECharts option（含 3D 地图 GeoJSON 处理、词云逐词着色等）。

#### 视觉设计系统（前端 `theme/`）
- **Galaxy AI Analytics** 暗色体系，单一色源 `Palette.ts`（SSOT，禁止在别处写死颜色）：
  - 数据主色 **星光蓝 #38BDF8**（占图表约 90%）
  - AI 语义色 **银河紫 #8B5CF6**（仅用于 Insight / 推荐 / Agent，禁入普通图表）
  - 交互色 **极光青 #22D3EE**（Hover / 选中 / 筛选 / 按钮）
  - 背景 深空 #020617、卡片 #0F172A，10 色有序分类色板（蓝→靛→青 冷色打头，暖色前置拉节奏）。

### 2.4 关键数据流

```
用户上传文件
   → SessionManager 持有 pandas DataFrame（按 session_id）
   → 清洗接口改写该 DataFrame
   → 分析接口：intent → Planner.plan → Template.execute → AnalysisPackage
        → chart_renderer → ECharts option（返回前端渲染）
        → 保存为 AnalysisPackage（session.saved_packages）
   → 驾驶舱接口：packages → dashboard.build_dashboard_schema
        → DashboardSchema（含交互）→ 前端 DashboardRenderer 渲染
   → 报告接口：packages → ai_agent.generate_report_from_packages
        → 结构化 sections（含 chartIndex 绑定图表）→ 前端渲染 / 导出
```

统一输出协议 **`AnalysisPackage`** 是贯穿"分析 → 图表 → 驾驶舱 → 报告"的中枢数据契约。

### 2.5 工程实践亮点

- **多 AI 服务商 OpenAI 兼容接入**：前端 `AI_PROVIDERS` 配置（DeepSeek / 通义千问 / 智谱 GLM / 月之暗面 Kimi / OpenAI / Agnes AI 等），后端仅透传 `base_url` / `api_key` / `model`，新增服务商零后端改动。Agnes AI（baseUrl `https://apihub.agnes-ai.com/v1`，模型 `agnes-2.0-flash`）已接入。
- **降级策略**：LLM 关闭 SDK 重试（`max_retries=0`）并确保超时即抛错 → 立即走统计降级；报告采用异步任务 + 轮询，不可能因 AI 慢而让前端超时。
- **双层数据防护**：
  1. 模板层四步模式（`_safe_divide` / `_safe_pct_change` 等边界处理）；
  2. 全局 `sanitize_json`（`src/utils/json_serializer.py`）统一清洗 NaN / inf / numpy / Timestamp / Decimal，5 个高风险路由返回前必过此层。
- **知识驱动、规则与代码分离**：所有业务知识集中在 `analysis_library/*.yaml`，引擎代码保持通用。

---

## 三、部署架构

项目采用**前后端分离 + 各自托管**的部署形态：

| 部分 | 托管方 | 配置 | 说明 |
| --- | --- | --- | --- |
| 前端（静态） | Vercel | `vercel.json` | 构建 `frontend/dist`；`/(?!api).* → index.html` 实现 SPA fallback |
| 后端（API） | Render | `render.yaml` | Python Web 服务；build 时 `pip install -r requirements.txt` 并构建前端 `dist`；start 用 `uvicorn main:app` |

- **本地开发**：前端走 Vite dev server（`npm run dev`），后端 `uvicorn main:app --reload`（端口 8001）。
- **生产可单实例托管**：后端 `main.py` 自带 `root` / `serve_spa` 路由，可直接托管 `frontend/dist`（Render 构建前端后由后端一并发静态资源），`index.html` 强制不缓存以保证拿到最新版本。
- **上传限制**：默认 `MAX_UPLOAD_SIZE_MB=50`（`config.py`），本地 `.env` 可覆盖到 5GB（`.env` 不入库）。

---

## 四、目录结构速览

```
数据分析项目/
├── config.py                  # 应用配置（标题/上传上限/支持格式/AI 参数）
├── vercel.json                # Vercel 前端静态托管 + SPA fallback
├── render.yaml                # Render 后端服务定义
├── backend/                   # FastAPI 后端
│   ├── main.py                # 应用入口 + SPA 托管 + 全局异常处理
│   ├── routers/               # upload/data/clean/stats/chart/dashboard/
│   │                          #   insights/report/analysis/reasoning
│   ├── services/              # SessionManager（会话数据）
│   └── utils/                 # AI 错误增强等
├── frontend/                  # React + TS 前端
│   ├── src/
│   │   ├── pages/             # UploadPage/CleanPage/AnalysisPage/DashboardPage
│   │   ├── components/        # EChartView/GLMapView/VisualizationRenderer/
│   │   │                      #   DashboardRenderer/BigScreen/...
│   │   ├── contexts/          # DataContext（全局状态 + AI_PROVIDERS）
│   │   ├── theme/             # Palette.ts（唯一色源）
│   │   └── api/               # Axios 客户端
│   ├── package.json           # 前端依赖与脚本
│   └── vercel.json
├── src/                       # 核心分析引擎（纯 Python）
│   ├── analysis_library/      # YAML 分析知识中心（14 类意图）
│   ├── analysis_templates/    # 14 个分析模板 + base(AnalysisPackage)
│   ├── calculators/           # 统计计算
│   ├── reasoning/             # 业务推理管道（rule/evidence/llm）
│   ├── dashboard/             # 自动驾驶舱（语义/布局/交互引擎）
│   ├── report/                # 报告分析器 + 构建器
│   ├── ai_agent/              # DataAnalysisAgent（对话/洞察/报告）
│   ├── planner.py             # 纯调度器
│   ├── echart_generator.py    # AnalysisPackage → ECharts option
│   ├── chart_renderer.py      # 图表渲染
│   └── utils/json_serializer.py  # 全局 sanitize_json 防护
├── static/                    # 静态样式
├── tests/                     # 测试
└── docs/                      # 内部设计文档
```

---

## 五、快速开始（本地）

```bash
# 1. 后端
cd backend
pip install -r requirements.txt
# 可选：在项目根目录创建 .env 设置 MAX_UPLOAD_SIZE_MB=5120（本地大文件）
uvicorn main:app --reload --port 8001

# 2. 前端（另一终端）
cd frontend
npm install
npm run dev          # 默认 http://localhost:5173

# 3. 使用
# 打开前端 → 上传表格 → 侧边栏选择 AI 服务商并填入 API Key（可选）
# → 依次体验 清洗 / 分析 / 驾驶舱 / 生成报告
```

> 说明：纯统计模式无需任何 API Key 即可运行；填入 Key 后解锁 AI 对话、洞察与报告。

---

## 六、备注

- 本文档配套的项目名为 **DataMind AI - 数据分析智能体**（`config.py` 中 `APP_TITLE`）。
- 仓库当前无 `README.md`；本文件 `项目介绍.md` 作为整体说明文档，与代码相互独立，可安全提交而不覆盖任何源码或既有文档。
- 关键设计决策（如 Agnes AI 服务商接入、词云/地图渲染细节、双层防护）均已在代码与团队记忆中有据可查，本文仅作整体概述。
