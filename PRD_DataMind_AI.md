# DataMind AI 产品需求文档（PRD）

> 版本：v1.0 ｜ 作者：[你的名字] ｜ 日期：2026.xx ｜ 状态：评审稿
> 关联原型：`prototype_data_mind.html`

---

## 1. 产品概述

### 1.1 一句话定位
DataMind AI 是一款**零门槛 AI 数据分析产品**：用户上传一张数据表，系统自动完成清洗、分析、可视化，并生成一份带业务结论的自然语言报告——把"数据分析师几小时的工作"压缩成"上传 → 等几十秒 → 拿走报告"。

### 1.2 背景与痛点
- **非技术用户有数据但不会分析**：运营、销售、学生手里有 Excel，却只会做透视表，看不出业务问题在哪。
- **会分析但重复劳动重**：分析师每次交付都要重复"清洗 → 挑指标 → 画图 → 写结论"，耗时且低价值。
- **直接问 ChatGPT 不可信**：把表格丢给通用大模型，它会算错数、编造不存在的数字，结论无法用于汇报。

### 1.3 目标用户
| 角色 | 特征 | 核心诉求 |
|---|---|---|
| 业务人员（运营 / 销售 / 学生） | 有数据，无分析能力 | 快速得到可读、可信的结论 |
| 数据分析师 | 会分析，嫌重复 | 把重复劳动自动化，聚焦高价值判断 |
| 管理者 | 只看结论 | 一份能直接拿去汇报的报告 |

### 1.4 产品目标
- **核心价值**：让"数据分析"像"搜索"一样简单、可信。
- **差异化**：数字由系统**精确计算**，LLM 只做自然语言转写，**从架构上杜绝幻觉**。
- **北极星指标**：单用户平均周分析报告生成数。

### 1.5 范围
- **做（本期）**：上传解析、智能清洗、11 类分析意图识别、自动可视化、业务推理、报告生成、看板驾驶舱。
- **不做（本期）**：实时数据库连接、多人协同编辑、自定义模型微调、付费订阅体系。

---

## 2. 用户画像

**画像 A · 小林（电商运营）**
- 24 岁，每日导出店铺订单 CSV，需要知道"哪类商品在涨、哪片区域在掉"。
- 痛点：Excel 只会做柱状图，看不出趋势背后的业务原因。
- 期望：上传后直接拿到"本周该盯哪三个指标"的结论。

**画像 B · 老王（初级数据分析师）**
- 30 岁，每周要交 3 份业务复盘报告，60% 时间花在清洗和画图。
- 痛点：重复劳动多，希望把"脏活"自动化，自己只做解读。
- 期望：系统出初稿，他在此基础上补充判断。

---

## 3. 核心用户流程

```
上传 CSV/Excel
   → 自动解析 + 列分类(数值/类别/时间/地理)
   → 智能清洗(缺失值/异常值/重复行, 可触发 AI 建议)
   → 分析意图识别(11 类)
   → 指标计算 + 自动可视化
   → 业务推理(带可信度标注)
   → 报告生成(异步)
   → 看板驾驶舱查看 / 导出
```

---

## 4. 功能需求清单

| 编号 | 模块 | 功能 | 优先级 | 说明 |
|---|---|---|---|---|
| F-01 | 上传 | 文件上传与解析 | P0 | 支持 CSV/Excel，自动识别列类型 |
| F-02 | 上传 | 数据预览 | P0 | 展示前 N 行与基础统计 |
| F-03 | 清洗 | 异常检测 | P0 | 缺失值 / 异常值 / 重复行自动标出 |
| F-04 | 清洗 | 一键清洗 | P0 | 按建议规则批量处理 |
| F-05 | 清洗 | AI 清洗建议 | P1 | 对复杂脏数据给自然语言建议 |
| F-06 | 分析 | 意图识别 | P0 | 自动判定适用 11 类分析意图 |
| F-07 | 分析 | 指标计算 | P0 | 统一数据契约输出 KPI/Chart/Table |
| F-08 | 分析 | 自动可视化 | P0 | 按数据选 ECharts 图表类型 |
| F-09 | 推理 | 业务推理 | P0 | 三段式(规则→证据→LLM)，带可信度 |
| F-10 | 报告 | 报告生成 | P0 | 异步生成，结构化自然语言 |
| F-11 | 看板 | 驾驶舱 | P1 | 多图组合 + 筛选 / 下钻 |
| F-12 | 看板 | 3D 地图 / 大屏 | P2 | 地理数据可视化 |

---

## 5. 关键功能详细说明

### 5.1 智能分析意图识别（F-06，P0）
- **描述**：系统读取数据结构后，从 11 类意图（增长 / 排名 / 对比 / 占比 / 相关性 / 异常 / 分布 / 集中度 / 留存 / 结构 / 地理）中自动挑选适用项。
- **验收标准**：
  - 含时间列 + 数值列 → 至少触发"增长""趋势"类意图；
  - 含地理列 → 触发"地理"意图；
  - 新增意图类型 = 增加一个 YAML 配置，**零代码改动**。

### 5.2 业务推理引擎（F-09，P0）
- **描述**：三段式管线——
  1. **规则引擎**：把计算结果组织成事实图谱；
  2. **证据引擎**：为每个结论打 STRONG / MODERATE / WEAK 可信度，过滤无证据结论；
  3. **LLM 叙事**：仅把已验证结论转写为自然语言。
- **验收标准**：所有出现在报告里的数字必须可追溯至计算过程；无 Key / LLM 失败时降级为规则摘要，不崩溃。

### 5.3 报告生成（F-10，P0）
- **描述**：提交后返回 `task_id`，前端轮询进度，完成拉取报告。
- **验收标准**：单次生成控制在部署平台超时阈值内；并发通过信号量限流；AI 超时即走降级报告。

### 5.4 可视化驾驶舱（F-11，P1）
- **描述**：按业务主题分组布局，支持筛选 / 下钻 / 高亮联动。
- **验收标准**：图表类型由数据特征驱动，非硬编码；布局引擎按 12 栅格分配，视觉均衡。

---

## 6. 非功能性需求

| 维度 | 要求 | 设计对策 |
|---|---|---|
| 性能 | 报告生成不超时 | 异步 + 轮询，规避 50s 网关超时 |
| 稳定性 | LLM 抖动不崩 | 多级降级（AI → 纯统计） |
| 资源 | 多会话不 OOM | 原始数据落盘 + LRU 淘汰 + 后台清理 |
| 兼容性 | 主流浏览器 | React + ECharts 标准栈 |
| 安全 | 数据隐私 | 支持本地优先，敏感数据不出域 |

---

## 7. 数据指标与成功标准

- **北极星**：单用户平均周报告生成数。
- **过程指标**：
  - 上传解析成功率 ≥ 98%
  - 报告生成成功率（含降级）≥ 95%
  - 平均生成耗时 < 40s（P95）
  - 7 日留存率（目标 ≥ 25%）

---

## 8. 风险与边界

| 风险 | 影响 | 对策 |
|---|---|---|
| LLM 服务抖动 | 报告失败 | 多级降级 + 重试预算控制 |
| 大文件内存溢出 | 服务不可用 | 落盘 + 信号量限流 + LRU |
| 隐私合规 | 数据泄露 | 本地优先 / 可选不上云 |
| 分析结论偏差 | 误导决策 | 可信度标注 + 数据可溯源 |

---

## 9. 版本规划

- **M1（基础闭环）**：F-01~F-04、F-06~F-08、F-10 → 上传即可出分析与报告。
- **M2（智能增强）**：F-05、F-09 → AI 清洗建议 + 业务推理。
- **M3（呈现升级）**：F-11、F-12 → 驾驶舱 + 3D 地图 / 大屏。

---

> 本文档用于作品集展示与面试讲解，对应 `prototype_data_mind.html` 可点击原型。
