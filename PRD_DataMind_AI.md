# DataMind AI 产品需求文档（PRD）

> 版本：v2.0（按"产品经理写 PRD"逻辑顺序重构）｜ 作者：[你的名字] ｜ 日期：2026-07-15 ｜ 状态：评审稿
> 关联原型：`prototype_data_mind.html`（Axure 等效物 · 可点击 HTML 高保真）
> 关联项目根目录：`d:\数据分析项目`

---

## 0. 文档使用说明

本文档按照"产品经理写 PRD"的标准逻辑顺序组织，共四篇：

| 篇 | 章节 | 解决"什么" | 对应思维导图层级 |
|---|---|---|---|
| 第一篇 | **产品定位** | 战略层：为什么做、给谁做、做到什么程度 | 战略层 |
| 第二篇 | **产品形式** | 载体层：用什么形态承载这份 PRD 和原型 | 表达形式 |
| 第三篇 | **产品设计 4 要素** | 战术层：从功能范围到感知体验逐层细化 | 范围层 / 结构层 / 框架层 / 表现层 |
| 第四篇 | **产品框架** | 文档层：PRD 自身的章节契约 | PRD 章节契约 |

---

# 第一篇 · 产品定位（战略层）

> 回答"为什么做、给谁做、做到什么程度"。

## 1.1 产品背景

### 1.1.1 行业现状
- **非技术用户或初学者有数据但不会分析**：学生、数据分析初学者手里有 Excel，却只会做透视表，看不出业务问题在哪。
- **会分析但重复劳动重**：每次每次交付都要重复"清洗 → 挑指标 → 画图 → 写结论"，耗时且低价值。
- **直接问 ChatGPT 不可信**：把表格丢给通用大模型，它会算错数、编造不存在的数据，结论无法用于汇报。
- **分析结果割裂**：当前市面上数据分析的可视化大屏与分析报告需要在不同平台上完成，耗时耗力

### 1.1.2 一句话定位
DataMind AI 是一款**零门槛 AI 数据分析产品**：用户上传一张数据表，系统自动完成清洗、分析、可视化，并生成一份带业务结论的自然语言报告——把"数据分析几小时的工作"压缩成"上传 → 等几十秒 → 拿走报告"。

### 1.1.3 差异化承诺（架构层面）
> **数字由系统精确计算，LLM 只做自然语言转写，从架构上杜绝幻觉。**

这是本产品区别于"ChatGPT + 表格"方案的最根本承诺，也是后续所有架构决策的根（分清哪些ai可以做，哪些不能做）：
- 所有出现在报告里的数字**必须可追溯至计算过程**；
- LLM 只能"换皮"，不能"换数"；
- 无 API Key / LLM 失败时**降级为规则摘要**，不崩溃（兜底）。

## 1.2 产品目标

| 维度 | 目标 | 度量 |
|---|---|---|
| 核心价值 | 让"数据分析"像"搜索"一样简单、可信 | 用户能否不查文档完成首次分析 |
| 北极星指标 | 单用户平均周分析报告生成数 | ≥ 2 份/周（视为已"用起来"） |
| 可靠性目标 | 报告生成成功率（含降级）≥ 95% | 平台日志 |
| 体验目标 | 平均生成耗时 < 40s（P95） | 平台日志 |
| 留存目标 | 7 日留存率 ≥ 25% | 埋点 |

## 1.3 用户需求

### 1.3.1 目标用户分层

| 角色 | 特征 | 核心诉求 | 占比预期 |
|---|---|---|---|
| **业务人员**（运营 / 销售 / 学生） | 有数据，无分析能力 | 快速得到可读、可信的结论 | 60% |
| **数据分析师** | 会分析，嫌重复 | 把重复劳动自动化，聚焦高价值判断 | 25% |
| **管理者** | 只看结论 | 一份能直接拿去汇报的报告 | 15% |

### 1.3.2 典型画像

**画像 A · 小林（电商运营）**
- 24 岁，每日导出店铺订单 CSV，需要知道"哪类商品在涨、哪片区域在掉"。
- 痛点：Excel 只会做柱状图，看不出趋势背后的业务原因。
- 期望：上传后直接拿到"本周该盯哪三个指标"的结论。

**画像 B · 老王（初级数据分析师）**
- 30 岁，每周要交 3 份业务复盘报告，60% 时间花在清洗和画图。
- 痛点：重复劳动多，希望把"脏活"自动化，自己只做解读。
- 期望：系统出初稿，他在此基础上补充判断。

### 1.3.3 用户需求池（从画像推导出的原始需求）

| 编号 | 原始需求 | 来源画像 | 收敛后落到 |
|---|---|---|---|
| N-01 | 能把 Excel 传进系统 | A | F-01 |
| N-02 | 不懂脏数据也能处理 | A | F-03, F-04 |
| N-03 | 不懂指标也能看出业务问题 | A | F-06, F-09 |
| N-04 | 看不懂图表也能读懂结论 | A | F-08, F-10 |
| N-05 | 报告要能直接拿去汇报 | C | F-10, 表现层 |
| N-06 | 重复清洗画图要自动化 | B | F-03~F-08 |
| N-07 | 数字必须可信，不能是 AI 瞎编的 | A, B, C | F-09（架构契约） |
| N-08 | 能组合多图做驾驶舱 | B, C | F-11 |

### 1.3.4 本期范围

**做（本期）**
- 上传解析、智能清洗、11 类分析意图识别、自动可视化、业务推理、报告生成、看板驾驶舱。

**不做（本期）**
- 实时数据库连接、多人协同编辑、自定义模型微调、付费订阅体系、SSO 鉴权。

---

# 第二篇 · 产品形式

> 回答"用什么载体承载 PRD 和原型"。

## 2.1 PRD 文档形式选择

| 形式 | 优劣 | 本项目选择 |
|---|---|---|
| **Word** | 篇幅过长不易理解、缺少交互页面、版本对比困难 | ❌ 不采用 |
| **Axure（HTML 等效物）** | 偏图形化，形象易于理解、可点击交互、便于截图入作品集 | ✅ 采用 `prototype_data_mind.html` |

**结论**：原型采用 Axure 风格的可点击 HTML 高保真（`prototype_data_mind.html`），PRD 本身采用 Markdown（便于在 GitHub 作品集仓库直接渲染、版本对比）。

## 2.2 关联资产清单

| 资产 | 路径 | 用途 |
|---|---|---|
| 可点击原型 | `prototype_data_mind.html` | 高保真交互演示 |
| PRD 本文档 | `PRD_DataMind_AI.md` | 需求契约 |
| 项目源代码 | `d:\数据分析项目\src\`、`frontend\`、`backend\` | 实现参考 |
| 简历 | `简历_DataMind_AI实习生.md` | 作品集入口 |
| 答辩准备 | `面试答辩准备_DataMind.md` | 面试配套 |

---

# 第三篇 · 产品设计 4 要素

> 从战略收敛到体验，逐层细化。本篇对应 Jesse James Garrett 的 UX 层次模型。

## 3.1 范围层 · 功能规格与内容需求

> **回答"做哪些功能、各功能到什么颗粒度、按什么优先级"。**

### 3.1.1 功能需求清单（F-xx）

| 编号 | 模块 | 功能 | 优先级 | 说明 |
|---|---|---|---|---|
| F-01 | 上传 | 文件上传与解析 | P0 | 支持 CSV/Excel，自动识别列类型 |
| F-02 | 上传 | 数据预览 | P0 | 展示前 N 行与基础统计 |
| F-03 | 清洗 | 异常检测 | P0 | 缺失值 / 异常值 / 重复行自动标出 |
| F-04 | 清洗 | 一键清洗 | P0 | 按建议规则批量处理 |
| F-05 | 清洗 | AI 清洗建议 | P1 | 对复杂脏数据给自然语言建议 |
| F-06 | 分析 | 意图识别 | P0 | 自动判定适用 11 类分析意图 |
| F-07 | 分析 | 指标计算 | P0 | 统一数据契约输出 KPI/Chart/Table |
| F-08 | 分析 | 自动可视化 | P0 | 按数据选 ECharts 图表类型 |
| F-09 | 推理 | 业务推理 | P0 | 三段式(规则→证据→LLM)，带可信度 |
| F-10 | 报告 | 报告生成 | P0 | 异步生成，结构化自然语言 |
| F-11 | 看板 | 驾驶舱 | P1 | 多图组合 + 筛选 / 下钻 |
| F-12 | 看板 | 3D 地图 / 大屏 | P2 | 地理数据可视化 |

### 3.1.2 11 类分析意图（从 F-06 展开）

| 意图 | 触发条件 | 典型图表 |
|---|---|---|
| 增长 | 时间列 + 数值列 | 折线 / 同比环比 |
| 趋势 | 时间列 + 数值列 | 折线图 |
| 排名 | 类别列 + 数值列 | 柱状图 Top N |
| 对比 | 多类别 + 数值列 | 分组柱状 |
| 占比 | 类别列 + 数值列 | 饼图 / 环图 |
| 相关性 | 双数值列 | 散点 / 相关系数 |
| 异常 | 数值列 | 箱线 / 离群点 |
| 分布 | 单数值列 | 直方图 |
| 集中度 | 类别 + 数值 | 帕累托 / 占比 |
| 留存 | 时间 + 用户 ID | 留存矩阵 |
| 结构 | 类别 + 数值 | 树图 / 嵌套饼 |
| 地理 | 地理列 + 数值 | 中国地图 / 3D 柱 |
| **词云** | 任意文本列 | 词云图 |

> 新增意图类型 = 增加一个 YAML 配置，**零代码改动**（这是 F-06 的核心验收项）。

### 3.1.3 关键功能详细说明

#### 3.1.3.1 智能分析意图识别（F-06，P0）
- **描述**：系统读取数据结构后，从上述 11+ 类意图中自动挑选适用项。
- **验收标准**：
  - 含时间列 + 数值列 → 至少触发"增长""趋势"类意图；
  - 含地理列 → 触发"地理"意图；
  - 含文本列 + 类别列 → 触发"词云"意图；
  - 新增意图类型 = 增加一个 YAML 配置，**零代码改动**。

#### 3.1.3.2 业务推理引擎（F-09，P0）—— 核心差异化
- **描述**：三段式管线——
  1. **规则引擎**：把计算结果组织成事实图谱（`business_fact_graph`）；
  2. **证据引擎**：为每个结论打 STRONG / MODERATE / WEAK 可信度，过滤无证据结论；
  3. **LLM 叙事**：仅把已验证结论转写为自然语言。
- **验收标准**：所有出现在报告里的数字必须可追溯至计算过程；无 Key / LLM 失败时降级为规则摘要，不崩溃。

#### 3.1.3.3 报告生成（F-10，P0）
- **描述**：提交后返回 `task_id`，前端轮询进度，完成拉取报告。
- **验收标准**：单次生成控制在部署平台超时阈值内；并发通过信号量限流；AI 超时即走降级报告。
- **报告固定结构**（9 段）：overview → kpi → trend → structure → top → anomaly → conclusion → suggestions → next_steps。

#### 3.1.3.4 可视化驾驶舱（F-11，P1）
- **描述**：按业务主题分组布局，支持筛选 / 下钻 / 高亮联动。
- **验收标准**：图表类型由数据特征驱动，非硬编码；布局引擎按 12 栅格分配，视觉均衡。

## 3.2 结构层 · 交互设计与信息架构

> **回答"功能如何被组织成信息架构、用户如何一步步到达目标"。**

### 3.2.1 顶层信息架构

```
DataMind AI
├── ① 上传（Upload）         ← 入口
├── ② 清洗（Clean）          ← 必经，可跳过
├── ③ 分析（Analysis）        ← 核心
│   ├── 智能分析
│   ├── AI 洞察
│   ├── 业务推理
│   └── 报告
└── ④ 驾驶舱（Dashboard）    ← 出口
```

> 导航顺序即用户心智路径：**数据进来 → 数据变干净 → 数据被分析 → 数据被看**。4 步可线性完成，无岔路。

### 3.2.2 核心用户流程（端到端）

```
[用户操作]              [系统动作]                       [产出]
─────────────────────────────────────────────────────────────────
上传 CSV/Excel    →   解析 + 列分类(数值/类别/时间/地理)  →  F-01/F-02
                       智能清洗(缺失/异常/重复)            →  F-03/F-04
一键确认         →   触发 AI 清洗建议(可选)              →  F-05
点击"开始分析"   →   11 类意图识别 + 指标计算             →  F-06/F-07
                       自动可视化(ECharts)                →  F-08
                       业务推理三段式(规则→证据→LLM)       →  F-09
                       报告生成(异步)                      →  F-10
查看/导出报告    →   HTML 报告落地                       →  F-10
跳转驾驶舱       →   多图组合 + 筛选/下钻                 →  F-11
```

### 3.2.3 状态与异常流

| 状态 | 触发 | 用户体验 |
|---|---|---|
| 正常完成 | 所有链路通 | 显示完整报告 + 驾驶舱 |
| 无 API Key | 用户未配置 | 走降级：报告 = 规则摘要 + 图表，标注"AI 叙事未启用" |
| LLM 超时 | 服务抖动 | 走降级同上，并在报告头部加降级标记 |
| 解析失败 | 文件格式损坏 | 提示具体错误行号 + 列名，不让用户猜 |
| 数据缺列 | 无数值/无时间/无类别 | 自动派生（计数指标/合并日期/放宽文本），不直接判"不支持" |
| 全部不支持 | 极端情况 | 给出**该分析类型专属建议**（词云/趋势/排名/结构/地理/相关/异常各有专属话术），非笼统"请检查数值列" |

## 3.3 框架层 · 界面、导航与信息设计

> **回答"每个界面长什么样、按钮和区块如何摆放、信息密度如何分配"。**

### 3.3.1 全局导航（4 步线性 + 4 子 Tab）

| 层级 | 元素 | 位置 | 触发 |
|---|---|---|---|
| 顶导航（4 步） | ① 上传 / ② 清洗 / ③ 分析 / ④ 驾驶舱 | 顶部 Tab | 主流程推进 |
| 分析内 Tab（4 个） | 智能分析 / AI 洞察 / 业务推理 / 报告 | ③ 分析内 | 结果分类查看 |
| 操作按钮 | 上传 / 一键清洗 / 开始分析 / 导出报告 / 查看驾驶舱 | 各页底部 | 流程推进 |
| 全局元素 | 进度轮询 / 任务状态 / 降级标记 | 顶部通知条 | 系统状态 |

### 3.3.2 关键页面骨架

#### 3.3.2.1 上传页（F-01/F-02）
- 顶部：拖拽区（CSV / Excel 两种格式说明）
- 中部：已上传文件列表（文件名 / 行数 / 列数 / 大小 / 操作）
- 底部：`开始分析 →` 主按钮

#### 3.3.2.2 清洗页（F-03/F-04/F-05）
- 左：数据预览表（前 N 行）
- 中：异常检测面板（缺失率 / 离群点 / 重复行 卡片三块）
- 右：AI 清洗建议（折叠面板，按需展开）
- 底部：`一键清洗` 主按钮

#### 3.3.2.3 分析页（F-06/F-07/F-08 + F-09 + F-10）—— 4 子 Tab
| 子 Tab | 内容 | 主操作 |
|---|---|---|
| 智能分析 | 11 类意图卡片网格 + 自动可视化图表 | 切换图表类型 |
| AI 洞察 | 自然语言关键发现（按可信度排序） | 展开证据链 |
| 业务推理 | 三段式推理过程可视化（规则→证据→LLM） | 查看原始计算 |
| 报告 | 结构化报告预览（9 段固定结构） | 导出 / 驾驶舱 |

#### 3.3.2.4 驾驶舱页（F-11/F-12）
- 顶部：模板切换（command / grid / medical / report）
- 中部：12 栅格布局，按主题分组（核心指标 / 趋势 / 排行 / 明细 / 洞察）
- 交互：筛选器联动 / 图表下钻 / 高亮

### 3.3.3 信息密度与可读性

| 原则 | 落地 |
|---|---|
| 数字必带语境 | 每个 KPI 卡片附"对比基期 / 维度 / 时间窗口" |
| 结论必带证据 | 每条业务结论配 STRONG/MODERATE/WEAK 标 + 证据链接 |
| 图表标题语义化 | 由数据自动生成"X 维度下 Y 指标的 Top N"，禁止空白标题 |
| 错误信息可执行 | "X 列 14 个离群点疑似测试数据，建议剔除"，不只说"有异常" |

## 3.4 表现层 · 感知设计

> **回答"用户看到/摸到这个产品时的主观感受"。**

### 3.4.1 视觉设计系统

| 维度 | 规范 | 备注 |
|---|---|---|
| 主题 | 深空暗色（Galaxy AI Analytics） | 与"AI"心智一致 |
| 主背景 | `#020617`（深空蓝） | 营造"宇宙感" |
| 卡片 | `#0F172A` | 与主背景形成层次 |
| 品牌色 | 星光蓝 `#38BDF8`（数据语义） | 系列色首色 |
| AI 强调色 | 银河紫 `#8B5CF6` | 专属 AI / 辉光 / 按钮 |
| 交互色 | 极光青 `#22D3EE` | Hover / Focus / 联动 |
| 内容色 | 月光白 `#F8FAFC` | 文字主色 |
| 状态色 | success `#34D399` / warning `#FBBF24` / danger `#FB7185` | — |

### 3.4.2 图表色板（10 色有序分类）

> 2026-07-11 升级为"暖色前置"调色板，蓝→靛→青三冷色打头保品牌调，第 4 位起暖色前置拉节奏。

1. 星光蓝 `#38BDF8`
2. 靛蓝 `#818CF8`（替补 AI 紫，比浅靛蓝更饱和）
3. 极光青 `#22D3EE`
4. 金色 `#FBBF24`
5. 玫瑰粉 `#F472B6`
6. 珊瑚橙 `#FB923C`
7. 青柠绿 `#84CC16`
8. 淡紫 `#C084FC`
9. 天空蓝 `#60A5FA`
10. 湖水绿 `#2DD4BF`

> 银河紫 `#8B5CF6` **专属 AI / 辉光 / 按钮 / 交互**，**禁入**普通图表系列（图表第二数据位由靛蓝替补）。
> 禁彩虹、禁每图随机、禁默认 ECharts 调色板、禁蓝白交替、禁白色柱子。

### 3.4.3 发光层（Glow）

- 卡片 Hover/Focus：银河紫 `rgba(139,92,246, 0.35)`
- 重点强调：银河紫 `rgba(139,92,246, 0.45)`
- 浅紫点缀：`rgba(167,139,250, …)`
- 所有蓝色发光统一替换为银河紫体系（与 AI 语义对齐）

### 3.4.4 感知设计原则

| 原则 | 体现 |
|---|---|
| 内容、功能、美学结合 | 数字 + 图表 + 叙事三位一体，缺一即"裸" |
| 提升用户感知体验 | 关键节点加微动效（KPI 数字滚动 / 图表入场） |
| 品牌一致性 | 银河紫是 AI 唯一锚点，用户看到紫即知"AI 在说话" |
| 可信感 | 数字带证据、结论带可信度、报告带降级标记，让用户敢拿去汇报 |

---

# 第四篇 · 产品框架（PRD 文档契约）

> 回答"PRD 自己应该怎么写"——这是 PRD 文档本身的规范。

## 4.1 变更记录

| 版本 | 日期 | 作者 | 变更内容 |
|---|---|---|---|
| v1.0 | 2026.xx | [你的名字] | 初版：按 9 章传统结构（概述/画像/流程/功能/详细/非功能/指标/风险/规划） |
| v2.0 | 2026-07-15 | [你的名字] | **按"产品经理写 PRD"逻辑顺序重构**：拆为产品定位 → 产品形式 → 产品设计 4 要素 → 产品框架 4 篇；补全 UX 层次模型中 PRD 原缺的"框架层"和"表现层"；新增用户需求池 N-xx 与原 F-xx 的推导关系 |

> 控制变更：所有实质性修改必须在本表登记，保证项目可控。

## 4.2 产品概要

| 维度 | 内容 |
|---|---|
| **产品名称** | DataMind AI |
| **价值主张** | 零门槛、可信的 AI 数据分析——上传一张表，拿到一份能汇报的报告 |
| **核心功能** | 智能清洗 + 11 类意图识别 + 自动可视化 + 业务推理 + 报告生成 + 驾驶舱 |
| **特色卖点** | ① 数字由系统算，LLM 只转写（从架构上杜绝幻觉）；② 无 Key 也能用（降级报告）；③ 零代码新增意图（YAML） |
| **目标用户群** | 业务人员 / 数据分析师 / 管理者（三层画像 A/B/C） |
| **商业模式** | 本期不涉及（详见 1.3.4"不做"清单） |
| **版本节奏** | M1 基础闭环 → M2 智能增强 → M3 呈现升级 |

## 4.3 需求说明

### 4.3.1 业务流程说明
- 见 3.2.2「核心用户流程（端到端）」。

### 4.3.2 功能与内容的逻辑关系

```
用户需求 N-xx
    ↓ 收敛
功能 F-xx
    ↓ 拆解
4 要素层
├─ 范围层（F-xx 清单 + 优先级）
├─ 结构层（信息架构 + 端到端流程）
├─ 框架层（页面骨架 + 导航 + 密度）
└─ 表现层（视觉规范 + 感知原则）
    ↓ 落地
开发实现
```

> 任何 F-xx 都能向上追溯到至少一条 N-xx，向下落到至少一处 4 要素描述。

### 4.3.3 产品框架结构说明
- 本 PRD 采用"四篇"结构而非"九章"结构：产品定位 → 产品形式 → 产品设计 4 要素 → 产品框架。
- 第三篇按 UX 层次模型（范围/结构/框架/表现）逐层细化，是产品从战略到体验的完整推理链。
- 第四篇是 PRD 自身的契约：变更记录、概要、需求说明、全局说明、非功能说明。

### 4.3.4 产品需求规则与逻辑

| 规则 | 内容 | 落地位置 |
|---|---|---|
| R-01 | 数字可追溯 | F-09 业务推理 + sanitize_json 全局 |
| R-02 | LLM 降级 | 无 Key / 失败时走规则摘要，不崩溃 |
| R-03 | 零代码新增意图 | `analysis_library/*.yaml` + `planner.py` |
| R-04 | 派生列兜底 | 缺数值/缺时间/缺分类时自动派生，不直接判不支持 |
| R-05 | 图表类型由数据驱动 | 不硬编码，详见 F-08 |
| R-06 | 报告 9 段固定 | overview→kpi→trend→structure→top→anomaly→conclusion→suggestions→next_steps |
| R-07 | 报告图表容器高度固定 420px | 禁止用 max-height 截断 |
| R-08 | Section 与 chartIndex 绑定 | 没图即不渲染占位，不强行补图 |
| R-09 | 词云用 per-item 静态色 | 禁用 series 级 `textStyle.color=function`（echarts-wordcloud 2.1.0 不支持） |
| R-10 | 银河紫专属 AI | 不入图表系列色板 |
| R-11 | 提示语分类型专属 | 不写"请检查数值列和时间列"笼统话 |

### 4.3.5 界面设计与交互说明
- 详见 3.3「框架层」全章节。
- 关键交互态：Hover / Focus / Loading / Error / Empty / Degraded。
- 关键动效：KPI 数字滚动入场、图表入场动画、降级标记呼吸。

## 4.4 全局说明

### 4.4.1 全局公共规则
- **路由响应式**：所有 5 个高风险路由（analysis / data / stats / dashboard / chart）返回前必须经过 `sanitize_json()`，杜绝 NaN / inf / numpy / Timestamp / Decimal 污染。
- **双层防护**：① 模板层四步模式（数据计算→边界修复→Insight→输出 Package）；② 全局 sanitize_json 层。
- **降级统一**：AI 链路任意一环失败 → 降级为规则摘要 + 图表 + 降级标记，不抛错给用户。
- **日志规范**：所有 LLM 调用必带 `task_id`、`model`、`prompt_tokens`、`completion_tokens`、`fallback` 标志。

### 4.4.2 风险与边界

| 风险 | 影响 | 对策 |
|---|---|---|
| LLM 服务抖动 | 报告失败 | 多级降级 + 重试预算控制 |
| 大文件内存溢出 | 服务不可用 | 落盘 + 信号量限流 + LRU 淘汰 |
| 隐私合规 | 数据泄露 | 本地优先 / 可选不上云 |
| 分析结论偏差 | 误导决策 | 可信度标注 + 数据可溯源 |
| 词云颜色 bug | 图表全黑不报错 | per-item 静态色 + 后端强制重算 |
| 地图空白 | 地理图无法渲染 | 本地化 GeoJSON + CDN 降级 |

### 4.4.3 边界与不做项
- 实时数据库连接：本期不做
- 多人协同编辑：本期不做
- 自定义模型微调：本期不做
- 付费订阅 / SSO：本期不做

## 4.5 非功能说明

| 维度 | 要求 | 设计对策 |
|---|---|---|
| 性能 | 报告生成不超时 | 异步 + 轮询，规避 50s 网关超时 |
| 稳定性 | LLM 抖动不崩 | 多级降级（AI → 纯统计） |
| 资源 | 多会话不 OOM | 原始数据落盘 + LRU 淘汰 + 后台清理 |
| 兼容性 | 主流浏览器 | React + ECharts 标准栈 |
| 安全 | 数据隐私 | 支持本地优先，敏感数据不出域 |
| 可扩展性 | 新增意图零代码 | YAML 配置 + Planner 自动匹配 |
| 可维护性 | 高风险路由零异常 | 双层防护（模板层 + sanitize_json） |
| 可观测性 | LLM 调用可追踪 | 统一日志字段 + task_id 串联 |

---

# 附录 A · 关键数据指标与成功标准

| 指标 | 目标 | 度量方式 |
|---|---|---|
| 北极星 | 单用户周报告生成数 ≥ 2 | 埋点 |
| 上传解析成功率 | ≥ 98% | 平台日志 |
| 报告生成成功率（含降级） | ≥ 95% | 平台日志 |
| 平均生成耗时 P95 | < 40s | 平台日志 |
| 7 日留存率 | ≥ 25% | 埋点 |

---

# 附录 B · 版本规划

| 版本 | 范围 | 关键 F-xx |
|---|---|---|
| **M1 基础闭环** | 上传即可出分析与报告 | F-01~F-04、F-06~F-08、F-10 |
| **M2 智能增强** | AI 清洗建议 + 业务推理 | F-05、F-09 |
| **M3 呈现升级** | 驾驶舱 + 3D 地图 / 大屏 | F-11、F-12 |

---

> 本文档用于作品集展示与面试讲解，对应 `prototype_data_mind.html` 可点击原型。
> 重构说明：v2.0 严格遵循"产品定位 → 产品形式 → 产品设计 4 要素 → 产品框架"的逻辑顺序，**新增**了 v1.0 缺失的"框架层（导航/界面）"和"表现层（视觉）"两个 PRD 章节，使需求契约从"功能列表"升级为"从战略到体验的完整推理链"。
