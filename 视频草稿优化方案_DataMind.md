# 视频草稿诊断与优化方案

> 原视频：`C:\Users\23927\Desktop\视频.mp4`
> 本文件目标：把当前 16 秒横屏录屏，改成能发抖音的 9:16 竖屏版本。

---

## 一、视频基础信息

| 项目 | 现状 |
|---|---|
| 分辨率 | 3072 × 1440（横屏/超宽） |
| 时长 | 16.27 秒 |
| 帧率 | 30 fps |
| 画面类型 | 100% 纯产品录屏 |
| 真人出镜 | 无 |
| 口播/音频 | 无（静音） |
| 花字/字幕 | 无 |
| 结尾 CTA | 无 |

---

## 二、致命问题（必须改，否则发出去也浪费流量）

1. **横屏 vs 抖音竖屏严重不匹配**  
   抖音在手机上以 9:16 竖屏为主。你的 3072×1440 超宽屏录进去会被压得很小，上下大量黑边，用户根本看不清图表。

2. **前 3 秒没有“停 scroll”钩子**  
   第 0 秒就是工具上传页，画面静态、无文字、无真人，刷到的人会直接划走。

3. **没有口播 + 没有花字**  
   抖音是“有声 + 有字幕”环境，纯静音录屏完播率会低很多。

4. **内容节奏拖沓**  
   数据表停留 2 秒、报告页重复画面停留 4-5 秒，看板滚动又拖 4 秒，而真正亮眼的大屏图表只出现 2 秒。

5. **没有 CTA**  
   16 秒结束在 AI 报告文本页，没有引导用户去评论区、去体验。

6. **视频时长与口播不匹配**  
   你规划的 4 句口播大约需要 20-30 秒，但现在视频只有 16 秒。要么**精简口播**，要么**补录操作把视频拉到 25-30 秒**。

---

## 三、逐秒画面诊断

| 时间 | 当前画面 | 问题 |
|---|---|---|
| 0s | 数据上传页 | 太平，不是钩子，缺真人/强花字 |
| 1-2s | 数据表 | 表格字太小，竖屏上几乎不可读；停留偏长 |
| 3-4s | 大屏图表 | **全场最佳画面**，但只出现了 2 秒，应该放大停留 |
| 5-6s | 报告页（柱状图） | 与上一页过渡弱，重复感 |
| 7-8s | 报告页（另一个报告） | 内容重复，可删 |
| 9s | 报告页（甜甜圈图） | 可保留，作为“报告”例证 |
| 10s | 分析可视化页 | 可保留 |
| 11-14s | 看板滚动 | 节奏拖沓，信息重复，建议压缩到 2-3 秒 |
| 15-16s | AI 报告文本 | 文字太多太小，竖屏看不清；结尾没有 CTA |

---

## 四、推荐优化方案（3 档难度，选一档执行）

### 方案 A：效果最佳（推荐）—— 重拍竖屏真人 + 录屏混剪

- 第 1 镜：手机竖屏自拍，口播第 1 句（“我用 vibe coding 出了个数据分析工具……”）
- 第 2-3 镜：工具录屏，但用手机录屏或后期裁剪成竖屏，重点放大上传→图表过程
- 第 4 镜：真人/黑底，口播结尾 + 引导“评论区体验”

**优点**：抖音算法对真人出镜更友好，完播率和互动率更高。  
**缺点**：需要重新拍真人出镜。

---

### 方案 B：折中方案—— 横屏录屏改竖屏 + 画中画关键帧放大

不拍真人，但要在剪映里做以下操作：

1. 新建项目，画布设为 **1080 × 1920（9:16）**。
2. 把原录屏拖到时间轴，**缩放并裁剪**到只显示画面核心区。
3. 对关键操作（上传、图表生成、看板）添加**关键帧**，让画面随操作移动放大：
   - 上传时放大到上传按钮区域；
   - 图表生成时放大到图表区域；
   - 看板时放大到最漂亮的图表卡片。
4. 剪掉重复拖沓的段落（7-8s、11-14s 的重复看板滚动）。
5. 把总时长拉到 20-25 秒，匹配你的 4 句口播。

**优点**：不用重拍，直接能用现有素材。  
**缺点**：需要手动打关键帧，操作略繁琐，但效果比直接压横屏好 10 倍。

---

### 方案 C：最低限度补救—— 原片不动，只加花字 + 配音 + 裁剪竖屏

只是把原片按 9:16 裁剪，加配音和花字。  
**缺点**：录屏内容仍然看不清，而且前 3 秒没有真人，转化率低。  
**不推荐**，除非你真的没时间重剪。

---

## 五、建议的新 9:16 分镜脚本（按你 4 句口播匹配）

总时长目标：**20-25 秒**，竖屏 9:16。

| 时间 | 画面 | 口播 | 花字/特效 |
|---|---|---|---|
| 0-4s | 真人出镜（竖屏自拍）或工具首页动效 | 我用 vibe coding 出了个数据分析工具，可以上传表格自动出图表、出大屏、出报告 | 主花字：**vibe coding 出品 🧩**；副花字：**图表 / 大屏 / 报告** 依次快闪 |
| 4-10s | 录屏：上传文件 → 表格一闪 → 大屏图表出现 | 你看，表往里一丢，图表自己出、大屏自己出、报告自己出，零代码 | **表往里一丢 📥** → **图表 ✅** → **大屏 ✅** → **报告 ✅** → **零代码 🚫💻**；配合画面逐词弹跳 |
| 10-16s | 看板/报告高光页（用关键帧放大最漂亮的图表） | 说白了就是个数据分析 Agent，不懂编程也能用 | 大字：**真·数据分析 Agent**；副字：**不懂编程也能用 👌**；Agent 用故障/Glitch 入场 |
| 16-22s | 工具界面 + 评论区引导卡片 | 想要体验一下的小伙伴欢迎来试试 | 花字：**👉 体验地址在评论区** + **免费 · 自取**；加手指贴纸指向评论区 |

---

## 六、剪映专业版操作步骤（方案 B 详细版）

1. 新建项目 → 比例设为 **9:16**。
2. 导入 `视频.mp4`，拖到主轨道。
3. 选中视频 → 右侧「画面」→「裁剪」，把画面裁到只保留中间工具区域。
4. 选中视频 → 右侧「画面」→「缩放」，放大到 180%-250%（看你原画面多宽）。
5. 给视频打**关键帧**：
   - 移动播放头到需要放大的位置 → 调位置/缩放 → 点关键帧按钮；
   - 重复几次，让画面随操作移动。
6. 用 `Ctrl+B` 分割，删除拖沓段落（建议把 7-8s、11-14s 的重复看板滚动压缩到 1-2 秒）。
7. 加口播：
   - 方式一：自己录音，拖到音频轨；
   - 方式二：用剪映「文本朗读」把你的 4 句文案转成 AI 配音。
8. 用「文本 → 智能字幕」识别字幕，再逐句改成上面的花字内容和动画。
9. 结尾加 CTA 花字 + 手指贴纸指向评论区。
10. 导出：分辨率 1080×1920，帧率 30，码率 8-12 Mbps，格式 MP4。

---

## 七、优先改的 3 件事（如果只能做 3 件）

1. **把视频改成 9:16 竖屏**（必须，否则抖音不推）。
2. **前 3 秒加真人出镜或强钩子花字**（否则没人停下来）。
3. **结尾加 CTA 引导评论区**（否则流量来了也没转化）。

其余优化（口播、BGM、花字动画）可以之后再迭代。

---

## 八、备选建议：如果暂时不想重拍

- 直接把视频**缩短到 8-10 秒**，只保留最亮眼的 3 秒图表 + 3 秒看板 + 2 秒 CTA，配合 BGM 发抖音「纯展示向」视频。
- 文案走「极短极炫」路线：  
  口播：“我用 vibe coding 出了个数据分析工具，上传表格自动出图表、大屏、报告。想要试试的来评论区。”
- 这种短版本可以先用，等真人出镜拍好了再发长版。
