# DataMind AI · 面试答辩准备（后端 / 平台实习生）

> 用法：每条 = 面试官可能怎么问 + 可背诵的标准答案 + 加分补充。
> ⚠️ 标注 `[按你真实情况填]` 的地方**必须真实**，不要编造——面试官一追问就露馅。
> 核心原则：**能讲清「为什么这么设计」比「写过多少行代码」更重要**。

---

## 一、这个项目能干嘛（HR / 开场必答 —— 先讲价值，再讲技术）

> 面试官（尤其 HR）开场几乎必问："你这个项目是做什么的 / 有什么用 / 给谁用"。**先用大白话把价值讲清楚，别一上来甩技术名词。**

### 1️⃣ 一句话说清它是什么
> "DataMind 是一个 **AI 数据分析平台**：普通人不用写代码、不用懂 SQL，**上传一张数据表（CSV/Excel），系统自动帮你清洗、分析、画图，最后生成一份能直接看的业务分析报告**。相当于把一个数据分析师的活，压缩成'上传→等几十秒→拿报告'。"

### 2️⃣ 给谁用 / 解决什么痛点（HR 最想听这段）
> "它解决三类人的痛点：
> - **不会分析的人**（运营、销售、学生）：手里有数据，但只会做个透视表、画个柱状图，看不出业务问题在哪。DataMind 帮他们自动挖出结论。
> - **会分析但嫌烦的人**（分析师）：每次都要重复'清洗→挑指标→画图→写结论'，很耗时。DataMind 把几小时的重复劳动压缩到几分钟。
> - **想用 AI 又怕它瞎编的人**：直接把表格丢给 ChatGPT，它会算错、会编数字。DataMind 的**数字全是系统精确算出来的，大模型只负责把已验证的结论讲成人话**，所以不会算错。"

### 3️⃣ 它到底能做哪些功能（跟着用户操作走一遍，最直观）
> "用户实际能做这几步：
> 1. **上传**：拖一张 CSV/Excel 进来，系统自动解析、识别每列是数值/类别/时间/地理。
> 2. **清洗**：自动检测缺失值、异常值、重复行，一键清洗，还能用 AI 给清洗建议。
> 3. **智能分析**：系统自动判断这份数据该做哪些分析——**增长、排名、对比、占比、相关性、异常、分布、集中度、留存、结构、地理，共 11 类**，把关键指标都算出来。
> 4. **自动可视化**：根据数据自动挑合适的图表（用 ECharts），包括**3D 地图、词云、以及大屏驾驶舱**。
> 5. **业务洞察/推理**：不只是画图，而是**推理出业务结论**——比如'华东区增长主要靠新客拉动，但复购率在下滑'，而且每个结论都带**可信度标注**。
> 6. **一键生成报告**：把上面所有结论组织成一份自然语言的完整业务报告，可以直接给领导看。"

### 4️⃣ 和"直接问 ChatGPT"有什么不一样（差异化，面试官爱追问）
> "最大区别是**可信**。你把 Excel 丢给 ChatGPT，它会一本正经地算错数、编出不存在的数字。我这个系统把'算数'和'说话'彻底分开：**pandas + 规则引擎负责精确计算和验证，大模型只负责把已经算好、已经验证的结论翻译成人话**。所以它既有 AI 的表达能力，又不会犯 AI 乱编数字的毛病。"

---

## 二、为什么做这个项目（"背景/动机"必问 —— ⚠️ 必须是你真实的想法）

> ⚠️ **这题只有你本人知道真实答案，下面是几种诚实框架，挑一个你真的认同的，别硬背不属于你的动机。** 一旦编造（比如假装某段实习经历），面试官追问细节当场翻车。

**框架 A · 痛点驱动（最稳、最好接 HR 的开场）**
> "我自己平时也要处理数据，每次拿到一张表都要重复清洗、挑图、写结论，很枯燥。我就想：能不能做一个'上传数据就自动出分析报告'的工具，把这套重复流程产品化。这就是 DataMind 的起点。"

**框架 B · 学习驱动（想吃透全链路）**
> "我想真正搞懂一个 AI 应用从前端交互、后端服务到大模型调用是怎么完整串起来的。看零散教程学不透，不如自己从零设计一个完整系统，逼自己把每一层的取舍都想明白。"

**框架 C · AI 协作实验（最独特，配合你的开源 skill 特别加分）**
> "2025 年我开始重度用 AI 编程 Agent，但我发现很多人说的'AI 写的项目'其实很浅。我想验证一件事：**用自然语言到底能不能指挥 Agent 造出一个架构合理、可扩展、有生产级健壮性的完整系统**？这个项目就是我的实验场——过程中我还把和 Agent 协作修 bug 的方法论沉淀成了一个开源 skill（quality-assurance-sop）。"

> 💡 **推荐组合**：用 **A 开场**（HR 听得懂、有共鸣）+ 被深挖时补 **C**（展示你的 AI 编排能力和平台思维）。C 是你区别于其他候选人的杀手锏，因为你有真实开源仓库兜底。

---

## 三、30 秒电梯演讲（两个版本，看对面是谁用）

**HR / 非技术面试官版（产品优先，先讲价值）：**
> "我做了一个 AI 数据分析平台 DataMind：普通人上传一张数据表，系统就能自动清洗、分析、画图，最后生成一份能直接看的业务报告——相当于把数据分析师的活自动化了。它和直接问 ChatGPT 的区别是：**数字都是系统精确算的，AI 只负责把结论讲成人话，不会算错**。整个项目是我用自然语言驱动 AI 编程 Agent 完成的，我负责需求拆解、架构设计和技术决策。"

**技术面试官版（架构优先，先讲设计）：**
> "DataMind 是一个 AI 数据分析平台：用户上传 CSV，系统自动完成清洗、分析、可视化，最后用大模型生成业务报告。
> 我是用自然语言驱动 AI 编程 Agent 做的——我负责**需求拆解、架构设计和关键技术决策**，Agent 负责落地实现。
> 后端是 FastAPI，拆成 11 个路由模块、40 多个接口；自己设计了会话管理、异步报告、以及一套规则+大模型结合的分析推理引擎。
> 整个项目我最得意的是在'AI 可控 + 系统不崩'之间做的一系列工程权衡。"

---

## 四、技术深挖（对应简历每条）

### 1. 会话管理 SessionManager
**问：为什么原始数据要 pickle 落盘，而不是一直放内存？**
> "平台是单进程多会话，每个会话一张 DataFrame，全留内存会 OOM。所以我把**原始数据落盘（pickle），内存只保留一份工作集**。读取时从磁盘恢复，正常工作集小，内存可控。落盘失败（磁盘满/无权限）就回退保留内存副本，保证功能不丢——这是优雅降级。"

**问：并发安全怎么保证？多用户同时上传怎么办？**
> "SessionManager 是单例，所有访问走一把 `Lock`；会话用 UUID 标识，互相隔离。还做了容量治理：最多 50 个会话，超了按 LRU 淘汰最久未访问的；过期会话用**惰性清理 + 后台守护线程**定时回收，避免内存只增不减。"

### 2. 异步报告任务
**问：AI 报告为什么用异步 + 轮询，不直接同步返回？**
> "AI 生成报告可能要几十秒，而部署平台（Render）HTTP 有 50s 超时。同步会直接 504。所以我设计成：**提交即返回 task_id，前端拿 task_id 轮询 `/status` 接口**查进度**，完成再拉结果。后端无状态、可水平扩展，也不被网关超时卡死。"

**问：并发请求很多，AI 接口扛不住怎么办？**
> "加**信号量限流**，控制同时打给大模型/打满 CPU 的并发数；超出就排队或快速失败，而不是雪崩。"

### 3. 大模型调用稳定性
**问：你为什么主动关掉 SDK 的自动重试？**
> "DeepSeek SDK 默认重试 2 次。一旦模型慢，单次 180s 超时 × 3 次 = 最坏 540s，远超前端 300s 超时——前端早断了，后端还在重试，用户永远收不到结果。
> 关掉重试后：**超时即报错 → 立刻走降级报告**，保证后端在预算内返回 200，前端不再空等。"

**问：AI 挂了/没 Key，系统还能用吗？**
> "能。我设计了**多级降级**：AI 失败就回退到纯 pandas 统计生成报告；推理管道里 LLM 那段也是可选的，没有 Key 就走规则生成结构化摘要。所以'没网/没 Key'时系统降级但不崩溃。"

### 4. 分析 / 推理引擎
**问：为什么推理要分「规则引擎 → 证据引擎 → LLM」三段，不直接全丢给大模型？**
> "两个原因：一是**可控**，规则引擎先把数据算成事实、证据引擎给每个结论打 STRONG/MODERATE/WEAK 可信度并过滤无证据的，LLM 只做'语言组织'，不乱编数字；二是**可降级**，没 Key 也能出报告。把'算数'和'说话'分开，幻觉面小很多。"

**问：新增一种分析类型要改很多代码吗？**
> "不用。分析能力是 **YAML 知识中心驱动的**：在 `analysis_library` 加一个意图配置（增长/排名/相关性…共 11 类），注册进 registry，模板和计算器按统一 `AnalysisPackage` 契约对接，就能扩展——**新增分析基本零代码改动**。这就是平台化的设计。"

---

## 五、AI 辅助开发专项（你最担心的）

### Q：开发过程中有没有用到 RAG？/ 你会用 RAG 来辅助自己开发吗？
**先澄清（避免答偏）：** 面试官大概率问的是"产品用没用 RAG"。产品侧答案见下；但你自己想问的是"**我开发时能不能用 RAG 帮自己**"——这层也要会答。

**产品侧（诚实）：**
> "**我们这个产品本身没有用 RAG。** 大模型拿到的不是'检索出来的文档'，而是系统**自己算好的、已验证的结构化结论**（事实图谱 + 可信度标注），LLM 只负责把这些变成自然语言报告。
> 我特意没用 RAG，因为我们的场景是**结构化表格数据 + 固定分析意图**，用规则引擎 + YAML 知识中心比向量检索更可控、不会检索到错误文档再被 LLM 放大。"

**开发侧（你真正关心的，加分题）：**
> "开发时我确实在解决 RAG 想解决的核心问题——**怎么给 Agent 持续、准确的上下文**。我没上向量数据库那种重 RAG，而是用了更轻更可靠的'结构化外置记忆'：把项目约定写进 `rules.md`（Agent 每次必读的硬规则）、把复盘过的改动沉淀成计划文件和 quality-assurance-sop skill（我开源在 GitHub 的 Agent 质量保障 SOP）。
> 如果项目再大、文件再多，我会直接上真正的**代码 RAG**——把源码、rules、计划嵌成向量，让 Agent 改代码前先检索相关文件，进一步压低幻觉和改错。我对 RAG 的适用边界是清楚的：**中小项目用结构化 spec 就够了，大型代码库才值得上向量检索**。"

**加分补充：** "RAG 适合非结构化知识问答；我们这种'算数 + 叙事'的场景，结构化上下文比检索更准。但我理解 RAG 的原理和取舍，也知道怎么用它做代码检索增强——只是当前项目用更轻的方案达到了同样效果。"

### Q：开发时用到了哪些 skill / 工具？
> "工具是 **CodeBuddy**（AI 编程 Agent）。
> 我修 bug 时最怕 Agent 三种典型毛病：**rush（没理解就动手）、partial（只改表面症状）、false completion（编译过了就说修好了）**。后来我把自己的应对打法——先定位根因、最小改动、跑验证、防回归——和 Agent 一起**总结成了一个可复用的 skill，叫 `quality-assurance-sop`，已经开源在 GitHub（github.com/linziquan/everything_skill，MIT 协议）**。
> 这个 skill 本质是一套**五阶段工作流**：完整理解 → 详细计划 → 执行 → 验证 → 诚实报告；并要求每个结论打置信度标签（✅已验证 / ⚠️需手动测 / 🔍推测），根因必须追问至少两层 why。核心是**把验收权从 Agent 自检转移到我自己的外部校验**，从机制上防幻觉和改错。
> 此外我维护 `rules.md`（项目宪法硬备份）+ `plans/` 归档，三者一起组成我的'防翻车'体系。"

> ✅ **完全可验证**：skill 已公开在 GitHub（含 SKILL.md / 根因追踪模板 / 错误模式清单），面试现场能打开给他看；项目内 `.codebuddy/rules.md`、`.codebuddy/plans/`（B12/B13/B14 等）也在。
> ⚠️ 讲 skill 时聚焦这一个真实产物，不编造别的。

### Q：你是怎么让 Agent 准确实现你的想法的？
> "核心是**规格驱动（spec-driven），不靠灵光一现的 prompt**：
> 1. **先定架构，再定代码**：我先画清模块边界和数据流（比如 AnalysisPackage 这个统一契约串起模板、计算器、渲染层），把这些写进规格文档，Agent 实现时有'锚'。
> 2. **给上下文，不给幻觉空间**：让 Agent 改某块时，我会指明**具体文件路径 + 参照的现有实现模式**，让它照葫芦画瓢，而不是自由发挥。
> 3. **小步快跑、每步可验**：一次只让 Agent 做一块独立功能，改完立刻跑/看 diff，合格再下一步。不让它一口气改十个文件。
> 4. **要它先讲思路再写**：复杂改动我要求 Agent 先说'打算怎么改、改哪几个文件'，我确认无误再让它动手，避免跑偏。"

### Q：出现 bug 时你怎么做？
> "四步，而且**我始终掌握'为什么'，不让 Agent 黑盒修**：
> 1. **定位**：把报错/traceback 贴给 Agent，让它指出是哪层（路由 / 引擎 / 渲染）出的问题，我对照代码确认，不盲信。
> 2. **要根因，不要只要补丁**：明确要求 Agent'先解释根因'，我理解透了再让它修——防止它瞎改掩盖问题。
> 3. **最小复现 + 看 diff**：让它用最小输入复现，修完后**我逐行 review diff**，重点看有没有'顺手改了不相关的文件'。
> 4. **防回归**：修完加一个能触发该路径的校验/测试，下次不会再踩。
> 另外全程 **git 频繁提交做安全网**，Agent 改炸了一键回退。"

### Q：想添加新功能时怎么做？
> "先判断能不能**复用已有的扩展点**，而不是另起炉灶：
> - 加分析类型 → 往 `analysis_library` 加 YAML 意图 + 配一对 calculator/template，走统一契约，零/少代码。
> - 加接口 → 仿照现有 11 个 router 的模式（同一个 SessionManager、同一套响应结构）新增，保持一致。
> - 加可视化 → 在 Widget 体系里加一种渲染器，前端 `WidgetFactory` 按类型分发。
> 定好'加在哪、用什么契约、不破坏什么'之后，再让 Agent 按模式实现。这样功能越加越多，架构不被冲垮。"

### Q：Agent 会遗忘、幻觉、改错，你怎么解决 / 避免？
这是高频题，分三类答：

**① 遗忘（上下文丢了 / 忘了之前约定）**
> "核心是把'记忆'外置成文件，不靠聊天上下文。我实际做了三件事：
> - **`rules.md` 硬备份**：把项目所有约定（图表映射、Tab 分类、AI 不能做的事）写成 Agent 每次必读的规则文件，它忘不了；
> - **bug-fix skill**：把修 bug 的固定打法让 Agent 沉淀成可复用技能，换会话也不丢；
> - **计划文件归档**：每次较大改动留一份 plan（项目里 `.codebuddy/plans/` 一堆），下次接着干有迹可循。
> 这其实是用'结构化外置记忆'替代了 RAG 要做的事——给 Agent 稳定的、可检索的上下文，从源头防遗忘。"

**② 幻觉（编了不存在的函数 / 接口 / 数字）**
> "我用 `quality-assurance-sop` 的**置信度标签**来治：Agent 每个结论必须标 ✅/⚠️/🔍，凡是标 🔍 推测或 ⚠️ 未测的，我一律当'未证实'处理，不进系统。再上三道闸：
> - **让它读真实代码再写**：给确切文件路径和现有模式，减少它'猜 API'。
> - **跑起来验证**：凡是它 import 的模块、调用的接口，我都实际运行/检查，编出来的立刻暴露。
> - **对产品里的 LLM 输出也做结构化校验**：报告生成要求模型返回 JSON，我用 schema 校验 + 兜底解析（失败就降级），不让一段乱七八糟的文本进系统。"

**③ 改错（改了不相关的文件 / 越界 / 破坏别的）**
> "靠 `quality-assurance-sop` 的**五阶段 + 用户验证清单**来约束：重要改动必须先出详细计划，我批准才执行；改完我逐行 review diff，对照 skill 里的'用户验证清单'逐项确认（是否真理解、是否只动该动的、是否跑了验证），发现越界立刻 git 回退。小步 commit 做安全网。本质是**外部验收权**——不依赖 Agent 说'我改好了'。"

**一句话总结（这题的收尾金句）：**
> "我的原则是——**Agent 写代码，但我掌握架构和验收权**。所有关键决策在我手里，Agent 是受控的实现者，不是甩手掌柜。这样既享受 AI 的速度，又不被它的失误埋雷。"

---

## 六、主动抛出的加分点（面试官爱听）

- "我们登录/没 Key 也能跑，靠的是**多级降级**，不是靠堆算力。"
- "我在设计时就考虑了**平台化扩展**——YAML 驱动 + 统一契约，加能力不堆 if-else。"
- "内存和超时是我花力气最多的地方，因为'能跑'和'线上不崩'是两回事。"

---

## 七、绝对不要说 / 要小心的坑

- ❌ 不要说"我一行一行手写了 SessionManager / 推理引擎"——你讲不清实现细节会翻车。说"我**设计**了方案、主导落地"。
- ❌ 不要编造没用过的 RAG / skill / 框架名字。
- ❌ 被问"这段代码你自己写的吗"——诚实说"由 AI Agent 按我的规格实现，关键设计是我定的"，这反而是加分项。
- ⚠️ 如果面试官想看代码仓库，确保你对简历里写的每一句都能在代码里指出来（本准备里的技术点都已对照过实际代码）。

---

## 八、自测清单（背完后对着答）

- [ ] **能用大白话说清"这项目能干嘛、给谁用、解决什么痛点"**（HR 开场必答）
- [ ] **能说出为什么做这个项目**（框架 A/B/C，选你真实认同的）
- [ ] 能说清和"直接问 ChatGPT"的区别（算数 vs 说话分离，不会瞎编数字）
- [ ] 能不卡壳说出 30 秒电梯演讲（HR 版 + 技术版）
- [ ] SessionManager：落盘防 OOM / Lock / LRU / 后台线程 —— 闭眼能讲
- [ ] 异步轮询：为什么不用同步（50s 超时）
- [ ] 关 SDK 重试：为什么（防超时叠加）+ 降级兜底
- [ ] 三段推理：规则→证据(STRONG/WEAK)→LLM，为什么不全给 LLM
- [ ] RAG：产品没用，说的是为什么不用（结构化优于检索）
- [ ] 加功能：复用扩展点（YAML/router 模式/Widget 工厂）
- [ ] bug/幻觉/改错：外置记忆 + 读真码 + 跑验证 + 看 diff + git 回退
- [ ] skill：能说出 `quality-assurance-sop` 是什么、解决哪三类 Agent 毛病、五阶段怎么跑（并指 GitHub 给他看）
