---
name: 清理未实现的计划文件
overview: 只保留 .codebuddy/plans 中描述“已实现”工作的计划文件，删除描述“未实现/被拒/过时草稿/重复”的计划文件。本次共删除 9 个、保留 10 个。被删的仅是规划元数据 .md，不含任何代码、不被任何模块 import，因此删除不影响前后端任何运行行为。
todos:
  - id: delete-superseded
    content: 逐个删除 6 个被取代/未实现的草稿：galaxy-VDS未完成、数据分析Web未完成、fix-qwen-037未完成、fix-yaxis、AI报告深空暗色两份
    status: completed
  - id: delete-duplicate
    content: 删除 3 个重复/未落地计划：visualization-justify草稿、免费部署上线草稿、DataMind-V2最终改造计划(非终版)
    status: completed
  - id: verify-remaining
    content: 校验 .codebuddy/plans 剩余文件数=10，确认 10 个保留项均存在且 9 个目标均已移除
    status: completed
    dependencies:
      - delete-superseded
      - delete-duplicate
---

## 用户需求

用户要求清理 `.codebuddy/plans/` 目录下的老旧/重复计划文件，唯一判定标准是：**该计划所描述的工作在当前代码中是否已实现**。已实现的计划保留，未实现/被取代/重复的计划删除——与文件名中的「(未完成)/(已完成)」标签无关。

## 核心目标

- 删除 9 个描述「未实现 / 被取代 / 重复」的计划文件。
- 保留 10 个描述「已实现工作」的计划文件（作为已落地工作的记录）。

## 删除清单（9 个，均已用代码现状核实为未实现/被取代/重复）

1. `galaxy-visual-design-system_2434eb86(未完成).md` —— VDS 已由 `_3ba7afa3(已完成)` 实现，草稿被取代。
2. `数据分析智能体_Web_应用_7c6d3022(未完成).md` —— Streamlit+Plotly 旧构想，项目已走 React（`_2c4f7742` 已完成），被取代。
3. `fix-qwen-model-name-404_037fd232(未完成).md` —— 前提错误（qwen3.7-plus 实为有效模型），被 v2 版取代。
4. `fix-yaxis-label-color_1e68dfb1(未完成).md` —— `echart_generator.py` 无该 axisLabel 补丁，未实现。
5. `AI分析报告改为深空暗色主题_1e7cb5af(未完成).md` —— 导出 HTML 深色化被用户拒绝，未实现。
6. `AI分析报告改为深空暗色主题_极简版__7c51fd2e.md` —— 标注 completed 但 `exportEChartsDashboard.ts` 实测未改，误导，未实现。
7. `visualization-dashboard-justify-layout_be0a7b0c(未完成).md` —— 草稿，布局改造已由 GridRenderer/schema 同类工作覆盖，无独立实现。
8. `DataMind-V2-免费部署上线_9bb235a5(未完成).md` —— 部署未确认发生，草稿。
9. `DataMind_AI_V2_最终改造计划.md` —— 与「最终改造计划_终版」高度重合，保留终版即可。

## 保留清单（10 个，工作均已在代码中实现）

1. `删除B_C链路孤儿死代码_d858d02d(未完成).md` —— `src.report` 引擎包已删（全仓 0 引用）。
2. `dead-code-cleanup-verify_833a10cb(未完成).md` —— 死代码文件已在 git 工作树删除。
3. `DataMind-V2-架构升级_c3fe5937.md` —— `src/analysis_library/` 已实现。
4. `galaxy-visual-design-system_3ba7afa3.md` —— VDS（`ChartStyle.ts`/`SemanticColor.ts`）已实现。
5. `数据分析智能体_Web_应用_2c4f7742.md` —— React 应用已实现。
6. `medical-dashboard-remove-map_80c8ae2b(未完成).md` —— MedicalDashboard 地图区块已移除（已验证）。
7. `DashboardPage上桥-集成改造计划_842a391d(未完成).md` —— schema 第5选项 + DashboardRenderer 链路已通（已验证）。
8. `fix-qwen-model-name-404-v2_6510bf65.md` —— 错误提示已更新列出 qwen3.7-plus（已验证）。
9. `DataMind_AI_V2_详细设计说明书.md` —— 已落地的系统设计规格。
10. `DataMind_AI_V2_最终改造计划_终版.md` —— 权威终版执行计划。

## 执行纪律

- 仅删除 `.codebuddy/plans/` 下的元数据 `.md` 文件，不涉及任何源码/配置，不影响前后端运行。
- 严守用户 2026-07-09 起纪律：仅本地删除，不 commit/push。