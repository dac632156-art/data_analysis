---
name: 分析报告死链路清理收尾
overview: 已删除 3 条未被使用的报告链路（后端 3 个端点、src/report_generator.py、前端 3 个函数与 1 个类型），用户确认不退回。静态核查确认均为死代码且无悬空引用。本计划补齐运行时冒烟测试与过期文档清理，确保删除安全闭环。
todos:
  - id: runtime-smoke-test
    content: 启动后端对 /report/ai-analyze 做运行时冒烟测试
    status: completed
  - id: clean-doc-refs
    content: 清理文档对已删 report_generator.py 的过期引用
    status: completed
  - id: verify-build
    content: 校验前端 tsc 与后端启动无 ImportError
    status: completed
    dependencies:
      - runtime-smoke-test
      - clean-doc-refs
---

## 用户需求

保留当前已删除状态（不执行 git restore、不推送 GitHub），完成「三条分析报告链路」清理的收尾与验证。用户规则澄清：仅禁止 push 到远程，本地 commit/restore 均允许；本次仅做本地改动，是否提交由用户决定。

## 清理范围（已执行，待验证）

- 后端 `backend/routers/report.py` 仅保留 `POST /report/ai-analyze`，删除 `/report/generate`、`/report/professional`、`/report/professional-advanced` 三个端点及其 helper。
- 删除孤儿模块 `src/report_generator.py`（全项目零 import）。
- 前端 `client.ts` 删除 `getProfessionalReport` / `getProfessionalAdvancedReport` / `generateReport`；`api.ts` 删除 `ReportResponse`；`DashboardPage.tsx` 删除未使用的本地 `generateReport`。

## 核心目标

1. 运行时冒烟测试确认 `/report/ai-analyze` 五阶段流水线正常，无 import 断裂。
2. 清理文档中对已删模块的过期引用。
3. 校验前后端构建与类型检查通过，确认无悬空符号。

## 诚实标注

静态核查（全项目 grep + 读码）已确认被删端点/函数/类型/模块零活引用、前端仅用 ai-analyze。运行时冒烟测试（目标 1）是唯一尚未执行的确认点，列为最高优先级。

## 技术栈

- 后端：FastAPI（Python）；报告引擎 `src.report_builder` / `src.report.*` / `src.report_analyzer`（保留并被 ai-analyze 复用）。
- 前端：React + TypeScript（`tsc` 类型检查）。

## 实现方案

删除动作已落地，本计划聚焦「验证 + 收尾」：

- **运行时冒烟测试**：启动后端，向 `/report/ai-analyze` 发送带真实 `session_id` + `api_key` 的 POST 请求，确认返回 `AIReportResponse`（含非空 `sections`），验证 DataMind 五阶段流水线未被删除动作破坏；重点确认移除 `report_generator.py` 后无 `ImportError`（ai-analyze 复用的引擎模块均已保留）。
- **文档清理**：仅删改 `面试准备_项目Bug与HR问答.md` 中对已删 `src/report_generator.py` 的引用（纯文档，不影响运行）。
- **构建校验**：前端 `tsc` 类型检查 + 后端启动导入检查，确认无悬空符号（如 `ReportResponse`、`getProfessionalReport` 等）。

## 关键决策

- **不退回、不推送**：保持已删除状态，仅本地验证；是否 git 提交由用户决定。
- **静态核查已完成**，运行时冒烟测试是本次唯一未执行的确认点，列为最高优先级，避免「编译/静态通过即声称完成」的虚假完成。

## 实现注意事项

- 冒烟测试避免空跑：检查响应 `sections` 非空、HTTP 无 500；若后端启动即报 `ImportError`，说明删除误伤了 ai-analyze 依赖链，需定位修复。
- 文档改动保持最小范围，仅删/改过期模块名（第 353、536 行附近），不重写章节结构。
- 全程只读验证 + 文档微改，不涉及业务逻辑改动，blast radius 极小。

## 目录结构

本次仅新增一处文档改动，其余文件已处于删除后待验证状态。

```
d:/数据分析项目/
├── 面试准备_项目Bug与HR问答.md   # [MODIFY] 清理第 353、536 行附近对已删 src/report_generator.py 的过期引用，避免误导。
├── backend/routers/report.py     # [已改-待验证] 仅留 /report/ai-analyze；冒烟测试确认无 import 断裂。
├── src/report_generator.py       # [已删-待验证] 静态确认全项目零 import，删除安全。
└── frontend/src/
    ├── api/client.ts             # [已改-待验证] 仅留 ai-analyze 调用；tsc 校验通过。
    ├── types/api.ts              # [已改-待验证] 仅留 AIReportResponse；tsc 校验通过。
    └── pages/DashboardPage.tsx   # [已改-待验证] 已删未使用本地 generateReport；tsc 校验通过。
```