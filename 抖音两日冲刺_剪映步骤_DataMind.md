# 剪映专业版 · 详细剪辑步骤（对照 6 镜分镜表）

> 适用：剪映专业版（Windows / Mac 桌面版）。手机版「剪映 App」按钮位置几乎一致，文末附差异提示。
> 目标视频：50 秒，vibe coding 数据分析工具展示。先拍好素材，再按下面 8 步剪。

---

## 一、开剪前：素材清单

把下面这些放进同一个文件夹，拍的时候照分镜来：

| 分镜 | 你要拍的素材 | 建议 |
|---|---|---|
| 1 | 真人出镜（对着镜头说）**或** 工具首页截图/录屏 | 不会露脸就用首页录屏，更省事 |
| 2 | 录屏：上传一张表 → 图表/大屏/报告依次出来 | 用 `业务数据.csv` 演示，录 10–15 秒有余量 |
| 3 | 真人出镜**或** 纯字幕画面（黑底/首页底） | 实在不想出镜，这镜用「字幕+底图」代替 |
| 4 | 网址卡片（见 Step 5，可后期做，不用拍） | — |
| 5 | 字幕强调画面（可复用首页/黑底） | — |
| 6 | 结尾画面（真人挥手 / 首页定格 / 黑底字幕） | — |

**收音建议**：口播最好现场戴耳机用手机/麦克风录，后期直接调音量；不想现场录，就用剪映「录音」功能后期配（Step 3 方式 B）。

---

## 二、8 步剪辑流程

### Step 1　新建项目 & 导入素材
1. 打开剪映专业版 → 点左上角 **「导入素材」**（或拖文件进左下媒体区）。
2. 选中所有素材（录屏、出镜、首页图）→ **「添加到时间轴」** 或拖到下方轨道。
3. 先随便排个序，后面 Step 2 再精确卡点。

### Step 2　排时间轴，按分镜卡点（关键）
目标时间轴（总 50s）：

```
0s ───── 3s ───── 12s ───── 22s ───── 30s ───── 40s ───── 50s
[镜1]      [镜2]       [镜3]       [镜4]       [镜5]       [镜6]
```

操作：
1. 把「镜 1（出镜/首页）」拖到时间轴 **0 秒起点**。
2. 播放头移到 **3 秒**处 → 选中该片段 → 按 **Ctrl+B（分割）**，删掉 3 秒之后的多余部分。
3. 把「镜 2 录屏」接在 3 秒后，拖到 **12 秒**处再分割删余。
4. 镜 3（12–22s）、镜 4（22–30s）、镜 5（30–40s）、镜 6（40–50s）同样用 **分割+删除** 卡齐。
5. 右下角把时间轴 **缩放滑块拉到最大**，方便精确对齐每 1 秒。

> 技巧：时间轴顶部有刻度，拖播放头时看左下角时间码（00:03 / 00:12…）对齐即可。

### Step 3　录 / 加口播（两种方式）
**方式 A（现场已收音）**：片段里已有声音 → 选中片段，右侧「音频」面板调音量到合适，开 **「降噪」** 一键去环境杂音。

**方式 B（后期配音，推荐新手）**：
1. 点顶部 **「音频」** → 左侧 **「录音」**。
2. 戴耳机，照分镜逐字稿念（见下方「逐镜口播对照」）。
3. 录完的音频自动进音频轨 → 拖到对应片段下方 **对齐时间**。
4. 口播比画面长/短 → 选中音频 → 右键 **「变速」**（1.0–1.3 微调）或裁剪长度。

### Step 4　加花字 / 字幕（最容易出效果的一步）
**最快做法 —— 智能字幕**：
1. 顶部 **「文本」** → 左侧 **「智能字幕」** → **「开始识别」**。
2. 剪映自动把口播转成字幕轨 → 逐个点开美化。

**手动加花字（按你的分镜表）**：
1. 选中对应片段 → **「文本」→「新建文本」**。
2. 输入分镜表里的 **花字**（见下表），字体选 **粗黑 / 综艺体**，颜色用 **亮黄或白+黑描边**，保证手机小屏看得清。
3. 右侧「动画」加 **入场动画**（打字机 / 弹入 / 放大），时长设 0.3–0.5s，别太长。
4. 把文本框拖到画面合适位置（一般上方 1/3 或居中）。

### Step 5　网址卡片（第 4 镜 22–30s）
推荐做法：**做一张图，比纯文字清晰**。
- 用 PPT / 美图秀秀 / Canva 做一张「体验地址：xxx」的卡片图（圆角矩形+大号字），导入当素材拖到 22–30s。
- 或剪映里：**「文本」→「新建文本」** 打网址 → 套 **「卡片」样式**（文本面板里选带底色的模板）→ 字号调大、居中。
- 加个 **「贴纸」** 里的箭头/手指，指向网址，引导点击。

### Step 6　转场（清爽为主）
1. 两片段交界处点 **「转场」**（时间轴两段中间的方块）。
2. 选 **「叠化」/「闪黑」/「运镜」** 这类短转场，时长设 **0.3–0.5s**。
3. 别用花哨特效，保持干练；6 个镜之间统一一种转场风格即可。

### Step 7　BGM & 音效
1. **「音频」→「音乐」** → 搜「轻快」「科技感」「无版权」→ 拖入音频轨。
2. 选中 BGM → 右侧音量压到 **20–30%**，别盖过口播。
3. 关键节点加音效：第 4 镜露网址、第 5 镜「3选1留评」时，从 **「音效」** 搜「叮 / 提示音」拖进去，强化记忆点。

### Step 8　封面 & 导出
1. 拖播放头到最吸睛一帧（建议 **第 1 镜真人/首页**，或录屏里图表炸出来的瞬间）。
2. 点 **「设置封面」** → 可加一句大字（如「vibe coding 出的数据分析工具」）。
3. **导出**：分辨率 **1080p**，帧率 **30**，码率「推荐」或自定义 **8–12 Mbps**，格式 **MP4**。
4. 导出完成后 → 直接「上传到抖音」，或在抖音 App 里发布（发布设置见原 `抖音两日冲刺_DataMind.md`）。

---

## 三、逐镜口播 + 花字对照（剪的时候照抄）

| 镜 | 时间 | 画面 | 口播（照着念） | 花字 | 操作要点 |
|---|---|---|---|---|---|
| 1 | 0–3s | 真人/首页 | 「我用 vibe coding 出了个数据分析工具，可以上传表格自动出图表、出大屏、出报告。」 | vibe coding 出品 🧩 | 花字放大抢眼，配弹入动画 |
| 2 | 3–12s | 录屏 | 「你看，表往里一丢，图表自己出、大屏自己出、报告自己出，零代码。」 | 可以干嘛·一闪 | 花字随录屏动作「快闪」出现 |
| 3 | 12–22s | 真人/字幕 | 「说白了就是个数据分析 Agent，不懂编程也能用。」 | 真·数据分析 Agent | 字幕居中，底色半透明 |
| 4 | 22–30s | 网址卡片 | 「地址我放评论区了，免费的，自取。」 | 👉 体验地址 | 卡片图居中偏大，加箭头贴纸 |
| 5 | 30–40s | 字幕强调 | 「顺手聊聊体验也行：好用、哪里别扭、想加点啥，三选一留个言。」 | 3选1留评 💬 | 关键词高亮，配「叮」音效 |
| 6 | 40–50s | 结尾 | 「你们说的我都记着，慢慢把它磨顺。」 | 慢慢磨顺 | 画面定格/淡出，BGM 收尾 |

---

## 四、手机版「剪映 App」差异提示
- 导入：底部「剪辑」→「开始创作」。
- 分割：选中片段 → 点「分割」（无快捷键，用手指拖播放头）。
- 录音：底部「音频」→「录音」。
- 智能字幕：「文字」→「识别字幕」。
- 其余步骤（花字、转场、BGM、导出）与桌面版一致，只是按钮在底部菜单。

---

## 五、3 个新手避坑
1. **口播和画面对齐**：录屏镜（镜 2）最容易口播比画面长，先剪画面再配口播，或口播用变速微调。
2. **花字别太多**：每镜只保留表里的那一句花字，堆字会显乱、手机看不清。
3. **网址必须放评论区 + 置顶**（抖音不允许视频里直接挂外链跳转，放评论区最稳，详见 `抖音两日冲刺_DataMind.md` 的置顶评论写法）。
