# 实习经历（AI 产品实习生简历素材）

> 用法：整段粘进简历「实习经历」栏；`[ ]` 占位替换为真实信息。
> 诚实底线：写你**真实做的事**，不夸大角色（如写"协调 / 分担标注"，不要写成"独立管理标注团队"）。本段是简历最强凭证，直接命中 JD 的"模型评测 / 效果评估 / BadCase 分析"。

---

**[公司名称] · [岗位，如 AI 数据 / 算法实习生] · [起止时间]**

- **数据治理与标注管理**：对客户端 SKU 图片执行下载与分类，剔除曝光异常、过暗、画面杂乱等低质样本；将有效数据划分为训练 / 测试集，协调标注实习生分工并分担标注，建立标注质检流程保障标注质量。
- **模型训练与部署**：基于 Python 登录服务器维护训练配置（继承历史权重、设定训练轮数、增量纳入新增 SKU 编码）并执行训练；训练产物经转换工具处理后部署至指定服务器，调用模型对测试集预打标。
- **模型效果评测**：对测试集人工质检后，将质检结果与模型预打标对齐评估，输出各 SKU 的精确率 / 召回率 / F1，以**总 F1 &gt; 95%** 作为上线量化门槛。
- **周检与归因分析**：每周对 B 端客户线上数据做质检评估，监控模型整体结果 &gt; 95%；针对单体 SKU F1 &lt; 95% 做归因分析并输出分析报告，形成"监控—归因—优化"闭环。

---

## 精简版（一页简历空间紧张时）

**[公司名称] · [岗位] · [起止时间]**

- **数据治理与标注管理**：对客户端 SKU 图片分类并剔除低质样本，划分训练 / 测试集，协调标注实习生分工并分担标注，建立标注质检流程。
- **模型训练与部署**：基于 Python 维护服务器训练配置（继承权重、设轮数、增量纳入新 SKU）并执行训练；产物经转换部署后调用模型对测试集预打标。
- **模型效果评测与周检**：人工质检对齐预打标，输出各 SKU 的 P / R / F1，以总 F1 &gt; 95% 为上线门槛；每周对 B 端线上数据做质检与归因分析，形成"监控—归因—优化"闭环。

---

## 答辩准备（仅你内部看，不进简历）

> 这段是真实经历，面试官必深挖。下面是你被问到时该怎么答，提前想熟。

- **"为什么定 95% 这个门槛？"**
  → 这是业务可接受精度与迭代速度的平衡：过低会影响 B 端客户体验、过高会拖慢模型上线节奏，是团队约定的上线标准。

- **"单体 SKU F1 低于 95% 你怎么归因？"**
  → 先定位是哪类问题：数据分布漂移 / 该类 SKU 样本量少 / 标注质量波动 / 模型泛化不足；定位后分别反馈给数据、标注或算法侧，形成改进闭环。

- **"你在这条链路里到底是什么角色？"**
  → 诚实答：我是**数据到上线全链路中的一环**（数据治理 + 标注管理 + 效果评测 + 周检），不是模型结构的设计者。这反而证明你懂模型从数据到上线的完整生命周期，对"模型效果负责"有体感——正是 AI 产品岗看重的。

- **"用了哪些工具？"**
  → Python、公司训练 / 标注平台、内部模型转换与部署工具（具体名称按你真实情况填，别编）。

- **"周检给谁看？"**
  → 给 B 端客户 / 内部业务方做模型质量保障（按你真实情况答，提前想好）。
